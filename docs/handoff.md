# 引き継ぎメモ（2026-08-25 その2・ISRの作り直し方を根本から変えた）

## 最重要: 前のメモの「削減の見込み」は**間違っている**

同じ日の前半（下の1本目のメモ／PR #97）で「再検証を900→3600秒に延ばしたのでISR Writesは
6万〜11万件に減る見込み」と書いたが、**この変更では1件も減らない**。救済措置をもらった直後に
検算して判明したので訂正した。訂正の内容は`docs/operations.md`の㉝「追記（同日・重要な訂正）」。

理由は算数。超過時の30日実測を1日あたりに直すと Edge Requests 10,300件・ISR Writes 9,882件・
ISR Reads 6,533件で、**書き込みが読み出しを上回っている**（リクエストの96%が再生成）。
sitemapの約7,051ページへ1日10,300リクエストが分散すると**1ページあたりの再訪間隔は平均16.4時間**
になる。revalidateがこれより短ければ訪問のたびに必ず期限切れ＝毎回書き込みになる。
900秒も3600秒も16.4時間より遥かに短いので結果は同じだった。

**この規模では時間ベースのISRという仕組み自体が長い裾に対して機能していない。**
「アクセスが薄いページほど、来訪のたびに作り直す」装置になっており、ページを増やすほど悪化する。

## 状況

- **本番サイトは復旧している**（08-25 14:11 UTCに`verify-production.yml`を手動実行してsuccess。
  SSRのHTMLも正常）
- ユーザーが救済措置を取得。**30日だけ通常の3倍枠**（ISR Writes 600,000 / Active CPU 12h /
  Provisioned Memory 1,080 GB-Hrs）。**30日後に通常枠へ戻り、超えていればまた停止する**
- PR #97 はマージ済み（08-25）。PR #104 が今回の訂正と本命の対策

## やったこと（PR #104）

1. **長い裾の`revalidate`を604800秒（1週間）に**（作品・声優・サービス別・シーズン・ランキング・
   独占配信）。`lib/annict.ts`のfetchも1週間＋タグ`"annict"`（ここが短いと実効値が引きずられる）
2. **鮮度は時間ではなく「指名」で取りに行く**。`app/api/revalidate/route.ts`が
   `revalidateTag("annict")`と現在クールへの`revalidatePath`を実行し、
   `.github/workflows/revalidate.yml`が1日2回叩く。認証は既存の`NOTIFY_CRON_SECRET`を
   使い回すので**新しいシークレットの設定は不要**
3. **長い裾の64%を事前生成に移した（最大の効果）**。事前生成したページはISR Writesを
   1件も消費しない（デプロイ成果物に含まれる）。事前生成していないページはデプロイのたびに
   キャッシュが消え、最初に見に来た人の分だけ必ず書き込みが起きる。**この床はrevalidateを
   いくら延ばしても消せない**。声優ページ4,483件＋過去シーズン64件を`generateStaticParams`で
   全件焼くようにした。事前生成は566→5,113ページ、**ビルドは54秒**（増分約20秒）
4. **OGP画像が完全に無キャッシュだった**。`app/anime/[id]/opengraph-image.tsx`は
   `force-dynamic`で毎リクエスト関数が起動し、中でAnnictとGoogle Fontsへ**計3往復**していた。
   I/O待ちはActive CPUには出ないがProvisioned Memoryには出るので、メモリ課金の主要な発生源の
   ひとつだったとみられる。明示の`Cache-Control`を入れた
5. **Route Handlerは`export const revalidate`ではなくCache-Controlヘッダが実効値**。
   埋め込みと`/api/work/[id]`が`s-maxage=900`のままで、1の変更が効いていなかった→86400
6. `scripts/check.ts`に「ISRの再生成頻度」節（12項目）。数字を戻す変更は画面を見ても
   気づけないので機械で見張る

## 削減の見込み（今度は式を書く）

| 指標 | 変更前（実測） | 見込み | 通常上限 |
|---|---|---|---|
| ISR Writes | 296,449 | 約 68,000 | 200,000 |

- ISR書き込みが残るページ: 7,051 − 4,547（事前生成へ）＝ 2,504
- そこへのクロール: 10,300 × (2,504/7,051) ＝ 3,658 件/日
- デプロイ間隔: 30日 ÷ 23回 ＝ 1.3日 → 間に4,771件（＞2,504なので全ページが1回は当たる）
- デプロイ由来: 2,504 × 23 ＝ 57,592
- 鮮度用14ページ（3600秒）: 14 × 24 × 30 ＝ 10,080（上限値）

Active CPU と Provisioned Memory は**ルート別の内訳がダッシュボードから見えないため定量化
できない**。再生成が約5分の1（9,882→約1,900件/日）になること、OGP画像とmiddlewareの往復が
消えることから方向は確実にプラス、というところまでしか言えない。

## ⚠️ 次のセッションが必ずやること

**30日枠の答え合わせ。** 通常枠に戻る前に実測で確認する。ダッシュボードの利用量グラフを
ユーザーに見てもらい、`docs/operations.md`の㉝に**予測ではなく実測**を追記する。
見込み68,000件に対して実際がどうだったか。外れていたら式のどこが違ったかを書く。

判定の目安（デプロイから1週間後）:
- ISR Writes が 1日あたり 2,500件を下回っていれば見込みどおり
- 上回っていれば、残っている作品ページ1,961件の事前生成を検討する
  （ただし`getWorkData`が過去クールでもAnnictへライブ取得する構造なので、
  焼くにはスナップショット優先へ切り替える必要があり、`credits`（キャラ名対応・監督・
  製作会社・原作者）が欠ける。スナップショットを`toAnimeDetail`まで含めて再生成すれば
  解決するが、要`ANNICT_TOKEN`＝PC作業）

## 手を付けていない次の一手

1. `app/anime/[id]/page.tsx`が共演声優の閾値判定と関連作品8件のためだけに`getSeasonData()`で
   クール全作品（最大224作品・casts込み）を取っている構造。エージェントの実測では、実際に読む
   のは6フィールドだけで**フルアイテムの22.9%**（2024-autumnで158KB→36KB・約77%削減）。
   ISR Writesには効かないがCPU/メモリには効く。表示の挙動が変わりうるので単独のPRで
2. `robots.txt`にCrawl-delayが無い。クロール量がそのまま書き込み量なので効くが、
   SEOと引き換え。`docs/seo-operations.md`の判断と突き合わせが要る
3. 過去クールの作品ページ1,961件の完全静的化（上の「判定の目安」参照）

## 前日（08-24）からの持ち越し

- Xの実測値（フォロワー数・インプレッション）は未取得のまま
- dアニメストアのアフィリエイト提携状況は未確認
- GSCの2週間推移。ただし停止していた期間のデータは信用できない

---

# 引き継ぎメモ（2026-08-25・Vercel利用上限超過への対応）

## 結論: 原因は「アクセス過多」ではなく「同じページを何度も作り直していた」こと

前日（08-24）の402停止は、Vercel Hobbyの30日利用上限の超過による**プロジェクトのPause**だった。
ユーザーがダッシュボードで確認した実測値は Fluid Active CPU 12h46s/4h・ISR Writes 296,449/200,000・
Fluid Provisioned Memory 489.1/360 GB-Hrs の3指標が超過。一方で Function Invocations 389K/1M・
Edge Requests 309K/1M・ISR Reads 196K/1M は**すべて上限内**だった。
訪問者の数は問題なく、**再生成の回数が問題**だったということ。

**ユーザーはアップグレードしない方針を決めた**（費用が発生するため）。よって利用量を上限内に
収める方向で対処した。詳細な原因分析と学びは`docs/operations.md`の㉝に書いた。

## やったこと（コード変更あり・全検査538件OK・ビルドOK）

1. **`vercel.json`を新規作成**（このリポジトリには存在しなかった）。`ignoreCommand`で
   `content/analytics/`・`content/coverage/`・`content/demand/`・`docs/`しか変更していない
   コミットは本番デプロイを起こさないようにした。**Vercelはデプロイごとに独立したISRキャッシュを
   持つ**ため、デプロイ＝キャッシュ実質全消去＝全ページ作り直しになる。mainへ自動コミットする
   cronは1日5本あり、うち3本はサイトの表示に一切使わないデータを書いていた。
   実際のコミット履歴12件で検証し、9件がスキップ・表示データを運ぶ`fetch-upcoming`だけが
   デプロイされることを確認済み。
2. **ISRの再検証を1時間（3600秒）に統一**。トップ900・シーズン600・作品900・声優900・
   ランキング600・独占配信600・サービス別600・`/api/work`900・埋め込み900・ICS900 → すべて3600。
   **これは表示を速くする変更**（キャッシュが効く時間が4〜6倍になる。ISRは期限切れ後も
   stale-while-revalidateで古いHTMLを即返して裏で作り直すので、待たされる場面は増えない）。
3. **データ層のTTLも3600へ**（`lib/annict.ts`の`next.revalidate` 600→3600、
   `lib/getSeasonData.ts`の`CURRENT_YEAR_REVALIDATE` 900→3600）。**ここが最大の落とし穴だった**:
   App Routerの実効revalidateは「ページの指定」と「描画中のfetch/unstable_cacheのTTL」の
   **低いほう**になるため、2だけでは実効900のままで無意味だった。
   `.next/prerender-manifest.json`を実測して発覚し、修正後は13ページすべてが3600になったことを
   同じ方法で確認した。**revalidateを変えたら必ずビルドして実効値を見ること**（CLAUDE.mdに追記済み）。
4. **`middleware.ts`が匿名リクエストでSupabaseへ往復しないようにした**
   （`lib/supabase/middleware.ts`に`hasAuthCookie`を追加）。Provisioned Memory 489.1 GB-Hrs ÷ 2GB
   （Hobbyの既定割当・変更不可）＝約245インスタンス時間に対し実CPUは12時間しかなく、
   **稼働時間の約95%がI/O待ち**だった。Vercelの課金はActive CPUがI/O待ちで止まる一方
   **Provisioned Memoryは止まらない**。middlewareは画像以外の全リクエストで`auth.getUser()`＝
   Supabaseへの往復を1回していた。ログイン中の利用者の挙動は一切変えていない。
5. **`warm-cache.yml`を毎時→6時間おき、`WORK_LIMIT`を60→30**。この巡回はページのrevalidate
   （900秒）より実行間隔（60分）が長かったため、**毎回必ずキャッシュ期限切れに当たり67ページを
   書き直していた**（30日で約46,000件＝ISR Writes全体の約16%）。「温め」ではなく
   「強制的な書き直し」になっていた。

## 削減の見込み（推定。Vercel側の内訳は見られないので実測で答え合わせが要る）

| 指標 | 変更前 | 上限 | 変更後の見込み |
|---|---|---|---|
| ISR Writes | 296,449 | 200,000 | 6万〜11万 |
| Fluid Active CPU | 12h 46s | 4h | 3〜4h |
| Fluid Provisioned Memory | 489.1 GB-Hrs | 360 GB-Hrs | 150〜250 GB-Hrs |

最も効くのは3（実効revalidateを4〜6倍に）で、ISR Writesの大半を占めていた
「7,051ページのISR面をクローラーと訪問者が15分ごとに再生成させていた」分が比例して減る。
**ただしこの内訳はVercelのダッシュボードから確認できないため推定**であり、
復旧後に実際の利用量グラフで確かめる必要がある。

## ⚠️ ユーザーにやってほしいこと（セッションからは実行できない）

1. **Vercelダッシュボードで手動 Resume する**。Hobbyの利用量は暦月ではなく**ローリング30日**で
   計測されるため、日が経てば古い分が落ちて自然に上限を下回る。**しかしPausedになった
   プロジェクトは上限を下回っても自動では復帰しない**。ダッシュボードで Resume を押す必要がある。
   **待っていても戻らない**。
2. Resume後、**このPRをマージしてデプロイする**。マージしないと上の対策は一切効かないため、
   同じ上限超過が再発する。
3. デプロイ後、Vercelの利用量グラフで ISR Writes の日次が下がったかを1週間ほど見る。
   下がらなければ`docs/operations.md`の㉝の「次の一手」を検討する。

## 次の一手（今回は手を付けていない。効果と危険度の順）

1. **`app/anime/[id]/page.tsx`が`getSeasonData()`でクール全作品を取っているのをやめる**。
   共演声優の閾値判定と関連作品8件のためだけに、シーズンページと同等の重いデータ
   （最大224作品・casts込み・Annict応答2MB超）を作品ページの描画のたびに読んでいる。
   今回revalidateを延ばしたことで頻度は1/4になったが、**構造としては残っている**。
   関連作品の抽出だけを行う軽量な索引に置き換えるのが本筋。ただし表示の挙動が変わりうるので
   単独のPRで慎重にやること。
2. **`robots.txt`にCrawl-delayが無い**。7,051ページを開放済みなのでクローラーの巡回速度が
   そのまま再生成の回数になる。ただしクロールを絞るとSEOに効くため、
   `docs/seo-operations.md`の判断と突き合わせてから決めること（安易に入れない）。
3. **過去クールのページを完全静的化する**。過去クールは`content/snapshots/`に確定データがあり
   内容が動かないので、原理的にISRである必要が無い。1,961作品＋4,483声優ページを
   ビルド時生成にするとBuild CPUと引き換えにISR Writesがほぼゼロになるが、
   ビルド時間が大幅に伸びるためBuild CPUの実測を見てから判断する。

## 前日（08-24）からの持ち越し

- Xアナリティクス（インプレッション・フォロワー数）の記入待ち（Issue #76/#95）。
- dアニメストアの提携申請状況の確認（`docs/affiliate-setup.md`）。
- GSCの08-17週の作品ページ表示急増が2週間続くかの答え合わせ。
  **ただしサイトが停止していた期間のGSCデータは信用できない**点に注意。

---

# 引き継ぎメモ（2026-08-24・日次巡回）

## ⚠️ 最優先: 本番サイトが約5時間以上ダウンしていた（HTTP 402・コード側の問題ではない）

**実測（GitHub Actionsのrunnerログから）**: 本日2026-08-24 18:07 JST頃から、本番
（`animedia-khaki.vercel.app`）の**全ルート**（トップ・`/api/season`全クール・`/season/*`・
`/rankings`・`/exclusive`・`/anime/*`）が**HTTP 402（Payment Required）**を返し続けている
（このメモを更新している23:23 JST時点でも継続中＝**6時間以上**）。応答時間は0.07〜0.1秒と極めて速く、
アプリのコード（Next.js側）が処理する前にプラットフォーム層で止められている挙動。
リポジトリ内に402を返すコードは無い（`grep -rn "402" app/ lib/`で確認済み）ので、
**コード修正で直る問題ではない**。

**原因確定（追記・ユーザーがVercelダッシュボードを確認）**: PR #97のVercelステータスチェックが
**`Account is blocked.`**（[vercel.com/knowledge/why-is-my-account-deployment-blocked](https://vercel.com/knowledge/why-is-my-account-deployment-blocked)へのリンクつき）
と返していた件、実体はプロジェクト`animedi`が**Hobby（無料）プランの利用上限超過によりPaused**
（画面表示「Upgrade to resume service」）だった。請求方法の期限切れや規約違反ではなく、
**3項目が上限超過**（他の項目＝Fast Origin Transfer・Function Invocations・Edge Requests・
ISR Reads・Web Analytics Events・Fast Data Transfer・Edge Request CPU Durationは全て上限内）:

| 指標 | 使用量 | 上限 | 超過率 |
|---|---|---|---|
| Fluid Active CPU | 11h 54m | 4h | 約3倍 |
| ISR Writes | 296K | 200K | 約1.5倍 |
| Fluid Provisioned Memory | 488.1 GB-Hrs | 360 GB-Hrs | 約1.4倍 |

Hobbyプランは上限超過で自動的にPauseされ、**Proへのアップグレード**または**利用量ウィンドウの
リセット待ち**（通常は課金月次サイクルでリセットされるが、このアカウントの正確なリセット日は
この環境からは確認できない）のどちらかでしか復旧しない。数時間待って自然復旧する種類の
障害ではない。

**`warm-cache.yml`が主要因である可能性が高い（要検証・以下は概算）**:
実行ログを数えたところ、`warm-cache`は**ほぼ毎時間**（08-23 15:41〜08-24 23:40の実測で
約1時間おき＝1日あたり約23回）起動しており、1回あたり**72リクエスト**を投げている
（内訳: トップ1＋`/api/season`×4＋`/season/*`×4＋`/rankings`1＋`/exclusive`1＋
ID取得用の`/api/season`1＋`/anime/{id}`×60）。このうち**ISRページは67件/回**。
`revalidate`は900s（15分）なので毎時の巡回は必ず失効後に当たり、**1ヒット＝1 ISR Write**になる。

- ISR Writes: 67件 × 23回/日 ≒ **1,541件/日 ≒ 46K/月**（上限200Kの約23%）
- Fluid Active CPU: 同じ1,541件/日がすべてサーバー描画を伴う。1描画あたり平均1秒と
  仮定すると約26分/日＝**約13時間/月**で、実測値**11h54m**とほぼ一致する
  （最大クール224作品のシーズンページなど重い描画があるため1秒は極端な仮定ではない）

**この一致は状況証拠であって実測ではない**（Vercel側の内訳を見ないと確定できず、
この環境からダッシュボードには到達できない）。ただしCPU超過分を説明できる規模の
負荷がこの巡回にあることは確か。**2026-08-05に5分おき→1時間おきへ落とした際、
GitHubのschedule間引きで実効2.5時間おきになっていたのが、現在は間引かれず
額面どおり毎時実行されている**点が当時の想定と変わっている（当時のコメントは
「実効性能を変えずに要求回数を1/12にする」だったが、いまは要求どおり動いている）。

削減するなら候補は3つ: ①`WORK_LIMIT=60`を下げる（60件が72リクエスト中の83%を占める）、
②cronを毎時から数時間おきに落とす、③`revalidate`（900s）を延ばして1ヒットあたりの
書き込み頻度を下げる。**ただし温めは表示速度のための仕組みで、落とせば初回アクセスの
体感が戻る**（`docs/operations.md`の⑦-4に経緯）。トレードオフの判断が要る。

**アップグレードするかリセットを待つかはユーザー判断**（費用が発生する意思決定のため
セッション側では決めない）。ユーザーへ確認を依頼済み。

**影響**: `daily-digest`（Bluesky/Threads自動投稿）が18:27 JST以降**6回連続失敗**（Issue #96、
23:22 JST実行時点でも継続）、`warm-cache`も**6回連続失敗**。一般訪問者も同時にサイトを
見られていない可能性が高い。

**通知**: 見つけ次第すぐにプッシュ通知で報告済み（このメモの記録より先に実施）。

**やったこと**: コード側に原因が無いことを確認しただけで、修正は行っていない
（Vercel側の対応が必要でユーザー作業）。`docs/handoff.md`にこの記録を残した。

**次回セッションへの申し送り**: 復旧していれば`verify-production.yml`の次回実行
（今夜05:36 JST頃）が緑に戻るはず。赤いままなら`production-check`ラベルのIssueが立つので
そちらを確認する。復旧後、`daily-digest`/`warm-cache`のIssue（#96）が閉じているか確認し、
閉じていなければ復旧確認のコメントを付けて閉じる。

## 今日のその他の点検（コード変更なし）

- `node scripts/seo-report.js`（GSC最終日2026-08-20）: 直近28日クリック31・表示1868・
  CTR1.7%・平均順位21.9位。前回（08-19時点23/1051/19.0位）から表示が急増しCTR・順位は
  悪化。週次推移（④）でも直近週（08-17週）は作品ページの表示が966（前週137から+829）と
  急増しているが、**1週間のデータでは季節性かノイズか判断できない**
  （`docs/seo-operations.md`の「1日の変動・1週間の変動で方針を変えない」ルールに従い、
  2週間以上の傾向が出るまで様子見）。声優ページの1ページあたりクリックは3.67で
  引き続き作品ページ（0.17）より優位。⑤⑥に新しい actionable な手がかり無し
  （11〜20位クエリは表示数が小さく、略称クエリも0クリックのまま）。
- `npm ci && npx tsc --noEmit && npm run check`（tsc→check.ts→周辺検査9本）**全件OK**。
  「シーズンページのHTML量」に`⚠`なし（最大クール2024-autumn・224作品・gzip推定79.4KB）。
  「descriptionの基準」節に前から出ている`⚠`（作品1961件中2件が削れない超過・最長129.5、
  作品名自体が長く削りようがない仕様どおりの警告）は新規ではない。
- GitHub側: PR #91（2026-08-21起票）・PR #94（2026-08-23起票）が未マージのまま滞留していた
  （どちらもhandoff.mdのみのドキュメントPR）。ユーザーから「判断を任せる」との回答を得たため、
  #94の内容（#91を取り込み済み・08-23/08-21/08-19/08-18/08-17の各エントリを保持）を
  このPR（#97）に統合したうえで、#91・#94はこのPRへの統合を理由にクローズした
  （3件が同じ内容を巡って並行するのを防ぐため。詳細は下記）。
- Issue #90（次クール準備・2026年秋）、Issue #95（本日のXの下書き）ともにコメント無し
  （Xのインプレッション・フォロワー数の記入は今回も無し）。Issue #76（週次X成長キット）も
  08-17から更新無し。
- アフィリエイト提携は引き続き3サービス（ABEMA・Prime Video・Hulu）で変化なし。

## このセッションについて

このセッションは日次巡回の指定ブランチ（`claude/daily-ops-YYYY-MM-DD`）ではなく、
実行環境から割り当てられた`cla/wizardly-knuth-ew3hse`で作業した（このブランチは
push時点でmainと同じコミットだったため、実質mainから新規に作業したのと同じ）。

## 目標の現在地（2026-11-06まで残り74日）

- Xフォロワー: 取得できず（今回もIssueへの記入なし）
- アフィリエイト成果件数: 取得できず（`/admin/analytics`はこの環境から到達不能）
- 提携済みサービス数: 3（ABEMA・Prime Video・Hulu、不変）

## 次回やること候補

1. **最優先**: 本番サイトの402が復旧しているか確認する（`verify-production.yml`の結果、
   またはIssue #96の続報）。復旧していなければ再度プッシュ通知で報告する。
   復旧にはユーザーのVercel側の対応（Proへのアップグレード or 利用量リセット待ち）が要る。
2. **アップグレードしない方針になった場合**: `warm-cache.yml`の削減を実施する
   （概算は上記のとおり、この巡回だけでFluid Active CPUの実測値をほぼ説明できる）。
   `WORK_LIMIT`を下げるのが最も効く（72リクエスト中60件＝83%）。
   **温めを止めると初回アクセスの体感速度が戻る**トレードオフがあるので、
   `docs/operations.md`の⑦-4を読んでから決めること。
3. GSCの08-17週の作品ページ表示急増が2週間続くか08-24〜08-27頃の取得で答え合わせする。
4. Xアナリティクスの記入待ち（Issue #76/#95）。
5. dアニメストアの提携申請状況の確認（`docs/affiliate-setup.md`）。
6. site-analyticsのサンプルが増えたら（`rowCount`・`officialClicks`合計が積み上がったら）
   未提携クリック順で`docs/affiliate-setup.md`の優先順位を実測ベースに更新する。

## ユーザーにやってほしいこと

- **Vercelの復旧方針を決めてください**（Proへアップグレードして即復旧するか、
  利用量ウィンドウのリセットを待つか）。Hobbyプランの上限超過によるPauseなので、
  どちらかを選ぶまでサイトは停止したままです。方針が決まればセッション側で
  利用量削減の調査（上記「次回やること候補」2）に着手できます。
- Xアナリティクス（インプレッション・フォロワー数）の記入。
