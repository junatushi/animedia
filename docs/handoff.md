# 引き継ぎメモ（2026-09-04・日次巡回・コード変更なし）

## 今日やったこと

`node scripts/seo-report.js`（GSC最終日2026-08-31）・`node scripts/freshness.js`・
`npm run check`（tsc→check.ts→9スクリプト全部）・`rm -rf .next && npm run build`
（+ローカル`next start`でSSR実地確認、`/season/2024/summer`のh1=1・作品リンク158件）を実行。
すべて exit 0・新規の`⚠`なし（「descriptionの基準」の既知⚠2件は変化なし、
「シーズンページのHTML量」に⚠なし）。外向き通信はプロキシが本番ドメインへのCONNECTを
403で拒否しており（`__agentproxy/status`の`recentRelayFailures`で確認）、本番の実地検査は
今回もローカルビルドに留めた。

- 全体の推移（①）: 直近28日でクリック+88・表示+7786（678→8464）、平均順位は20.6→23.5へ
  引き続き悪化。ただし08-31時点の直近週（08-24週・作品5/299・声優1/225）は`*`＝途中データで、
  `docs/operations.md`㉟の既存診断（表示急増は次クールsitemap追加の長い裾による希釈で、
  作品ページのクリック自体は落ちていない）の範囲内。新しい異常ではないと判断。
- ③面別効率: 声優2.11・作品0.95（前回0.90からわずかに改善）・制作会社が初めて表示を持った
  （8表示1クリック・11.8位）。⚠表示ゼロは監督/シーズン/独占で変化なし。
- 3節の判定表（監督・制作会社／声優過去クール拡張／略称・口語形）はいずれも判定日
  09-11〜09-16でまだ先。⑤⑥に単発で手を入れる明確な候補は無し。
- **site-analytics（09-04分）で`follow_click`が初めて非ゼロになった**（last30=4、09-03分までは
  30日間ずっと0）。`affiliate_click`3件（変化なし）、未提携`officialClicks`は引き続きnetflix・
  d_animeが最多（各5件/30日）。提携済みサービスは3（変化なし）。目標1（Xフォロワー100人）に
  直結しうる初めての動きだが、フォロワー数自体は今回も取得できず（Issue #131・#118とも
  コメント0件）。1日だけの変化なので継続して見ること。
- Issue #90（2026年秋クール準備）は09-03付けで既に別セッションが点検済み（配信0件の注目作3件、
  いずれも一次情報が無く追加見送り。sitemap反映も確認済み）。今回追加の対応なし。
- 開いているPRは #130（司令塔=Opus／実行=Sonnet分離、日次巡回とは別件・CI green）のみ。
  以前滞留していた4件（#127・#126・#124・#120）はすべてマージ済みだった。

## 目標の現在地（2026-11-06まで残り63日）

- Xフォロワー: 取得できず
- アフィリエイト成果件数: 取得できず（`/admin/analytics`はこの環境から到達不能）
- 提携済みサービス数: 3（ABEMA・Prime Video・Hulu、不変）

## 次回やること候補

1. `follow_click`の非ゼロが継続するか（09-05以降のsite-analyticsで確認）。
2. 3節の判定表（監督・制作会社／声優過去クール拡張）の判定日09-11〜09-12が近い。
   その週の`seo-report.js`実行時に判定すること。
3. Xアナリティクス・dアニメストア提携申請状況の確認（継続、今回も取得できず）。

---

# 引き継ぎメモ（2026-09-03・日次巡回・コード変更なし）

## 今日やったこと

`node scripts/seo-report.js`（GSC最終日2026-08-30）・`node scripts/freshness.js`・
`npx tsc --noEmit`・`node scripts/check.ts`・`npm run build`（+ローカル`next start`でSSR実地確認）
を実行。新規の`⚠`・コードの不具合は見つからなかった（「descriptionの基準」の既知⚠2件は変化なし、
「シーズンページのHTML量」に⚠なし）。`freshness.js`も失敗0件（既知の欠測2件のみ）。

- 全体の推移（①）: 直近28日でクリック+85・表示+7316（678→7994）と伸びは続いているが、
  CTRは2.4%→1.3%、平均順位は20.6→22.7へ悪化。ただし**08-24週は「作品」「声優」とも
  `days:7・partial:false`＝完全な週**（PR #120がまだ未マージのためレポート表示には出ないが、
  生データを直接確認した）なので、悪化は途中週のノイズではない。`docs/operations.md`㉟が
  記録した「表示急増は次クールsitemap追加による長い裾の指標な希釈で、作品ページのクリック自体は
  落ちていない」という既存の診断と整合する範囲内であり、新しい異常ではないと判断した。
- ③面別効率: 声優2.22・作品0.90（37ページ0.27から改善）・制作会社が初めてクリックを持った
  （1件）。⚠表示ゼロの面は監督/シーズン/独占。
- 3節の判定表（監督・制作会社ページ／声優ページ過去クール拡張／略称・口語形の露出）は
  いずれも判定日が2026-09-11〜09-16でまだ先。⑤⑥に単発で手を入れられる明確な候補は無し
  （cloverworks/engiの「アニメ一覧」型クエリは既に`lib/pageMeta.ts`のtitleパターンと
  一致しており、追加の変更点が見当たらない）。
- Xの数字: Issue #128（09-03下書き）・#118（週次成長キット）ともにコメントなし、
  引き続き取得できず。site-analytics（09-03分）: `follow_click`は30日で0のまま、
  `affiliate_click`3件（prime2・abema1）、未提携`officialClicks`はd_anime最多（5件/30日）で
  既存の優先順位（`docs/affiliate-setup.md`）と整合。提携済みサービスは3（変化なし）。
  外向き通信はプロキシが本番・Annict双方へのCONNECTを403で拒否（`__agentproxy/status`の
  `recentRelayFailures`で確認）していたため、ローカルビルドでの検証に留めた。

## ⚠️ 未マージのPRが4件に増えている

`claude/daily-ops-*`系・機能PRとも、CIはすべてgreenだが未マージのまま滞留:
- #127 Annictキャッシュタグを作品単位へ（Active CPU削減の本命・Vercel利用上限対応の続き）
- #126 Active CPUのルート別実測（対策候補1は効果なしと判明・記録のみ）
- #124 日次巡回の記録（09-02・コード変更なし）
- #120 seo-report.jsに「途中の週」の印を表示する

いずれも`mergeable_state`は`clean`または`unknown`（コンフリクトなし）。特に#127は
Vercel利用上限（Issue #123・3倍枠の答え合わせ）に直結する対策なので優先度が高い。
マージ判断はユーザー側（このセッションは日次巡回のみで他PRの作成者ではないため、
確認のみで手を出していない）。

## 次回やること候補

1. 上記4件のPRのマージ（特に#127・#123のVercel答え合わせ）。
2. 3節の判定表（監督・制作会社／声優過去クール拡張）の判定日が2026-09-11〜12に迫っている。
   その週の`seo-report.js`実行時に判定すること。
3. Xアナリティクス・dアニメストア提携申請状況の確認（継続、今回も取得できず）。

---

# 引き継ぎメモ（2026-09-02・コード変更なし。Vercel利用量の答え合わせIssueに注意）

## ⚠️ 最優先: Issue #123（Vercel利用量の答え合わせ・判定日2026-09-02）はユーザー確認待ち

`docs/operations.md`㉝の対策デプロイ（2026-08-26）から1週間の節目で、
`usage-check.yml`が本日Issue #123を起票済み（未コメント）。Vercelダッシュボードは
ログインが要るためセッションからは絶対に読めない。**「1日あたりISR Writes 2,500件未満」
などの合格ラインをユーザーがダッシュボードで確認し、Issueにコメントする必要がある**。
3倍枠は2026-09下旬に切れるので、次の判定日（2026-09-18・最終確認）までに対応が要る。
合格していればコメント後に`docs/operations.md`㉝へ実測を追記するのが次の作業。

## 今日やったこと・分かったこと（コード変更なし）

- **ブランチ**: 実行環境が割り当てたのは`cla/wizardly-knuth-uul4tg`で、これは
  push直前に`origin/main`（46da576）と同一コミットだった。08-24と同じ事情
  （下記の08-24エントリ参照）なので、そのままこのブランチで作業した。
- **`node scripts/seo-report.js`**（GSC最終日2026-08-29）: 直近28日クリック94
  （08-08時点16から+78）・表示7542（+6864）・CTR1.2%（2.4%から低下）・平均順位22.6位
  （20.6から悪化）。③面別1ページあたりクリックは声優2.25が引き続き最効率で、
  **制作会社が初めて表示回数を持った**（1ページ7表示1クリック・8.7位）。3節の判定表
  「監督ページ・制作会社ページ」（判定日2026-09-12）の続行条件に近づいた形（監督は
  まだ表示ゼロ）。作品は0.83（08-19時点0.27から上昇）だが、**直近週（08-24週）は
  GSC最終日が08-29までのため実質6/7日分の途中データ**（`docs/operations.md`㉟と同型の
  落とし穴）。この表示を機械的に警告するPR #120（週次推移に`*`印を付ける）はまだ
  未マージなので、今回は手動で「途中週の数字だけで方針を変えない」を守った。
  ⑤(b)に`cloverworks アニメ一覧`(10.3位)・`crunchyroll アニメ一覧`(10.9位)・
  `engi アニメ一覧`(11.0位)など制作会社・サービス別ページ向けと見られるクエリが
  10〜11位に複数固まっている。09-12の判定に効きうるので次回以降も追う。
  ⑤(a)は`/anime/10143`（20表示・25.3位）の1件のみで20位台＝順位の問題（title起因ではない）。
  ⑥略称クエリは引き続き1クリックのみ（判定日2026-09-16は変更なし）。
- **`node scripts/freshness.js`**: 失敗なし。GSCの08-23・08-28は既知の回収可能な欠測。
  他の収集（サイト行動ログ・track-season）は健全。
- **`npx tsc --noEmit && node scripts/check.ts`**: 全件OK。新規の`⚠`なし
  （「descriptionの基準」の作品2件超過・「シーズンページのHTML量」は既知で変化なし）。
- **`content/analytics/site/2026-09-02.json`**: `rowCount`が0から36に増え、初めて実測が付いた
  （2026-08-19時点は`rowCount:0`で未実測だった）。未提携クリック（`officialClicks`）は
  直近30日で d_anime 4・netflix 4・bandai 2・unext/fod/dmm/niconico/disney 各1で、
  `docs/affiliate-setup.md`が既に最優先としているdアニメストアの判断と整合。
- **X**: Issue #76（週次成長キット・08-17週）はコメント無しのままclose、Issue #118
  （08-31週）もコメント0件。フォロワー・インプレッションは今回も取得できず。
- **アフィリエイト**: `content/affiliate/programs.ts`は引き続き3サービス
  （ABEMA・Prime Video・Hulu）で変化なし。
- **外向き通信**: プロキシが本番・Annictとも403で拒否、本番HTML実地検証は今回も不可。

## 気になった点（対応はユーザー判断）

`claude/daily-ops-*`ブランチのPRが2件、CI green のまま未マージで滞留している
（#114=08-31分、#120=09-01分。どちらもdoc-only or 小さな表示修正）。実質的な機能PR
（#113・#115〜#121）は正常にマージされているので運用が止まっているわけではないが、
この2件は放置すると次回以降さらに古くなる。マージ／クローズはユーザー判断
（`docs/daily-ops.md`5節の方針どおりセッション側では判断しない）。

## 目標の現在地（2026-11-06まで残り65日）

- Xフォロワー: 取得できず
- アフィリエイト成果件数: 取得できず（`/admin/analytics`はこの環境から到達不能）
- 提携済みサービス数: 3（ABEMA・Prime Video・Hulu、不変）

---

# 引き継ぎメモ（2026-09-01・seo-report.jsの週次推移に「途中の週」の印を足した）は`docs/operations.md`の㉟（追記2026-09-01）を参照

`weeklyByType`の`partial`フラグがデータ層には入っていたのに`seo-report.js`④の表示に
反映されていなかった件（㉟と同型の誤りの再発防止）の詳細記録は`docs/operations.md`の
㉟に統合済み（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。
Vercel利用上限の30日枠の答え合わせは09-02のPR #125で完了済み。

---

# 引き継ぎメモ（2026-08-31・声優ページの索引規則の作り直し・次クールsitemap追加）は`docs/operations.md`の㉟を参照

GSC実測に基づく声優ページの索引規則の作り直し（今年は全部／過去年は総出演50作品以上）、
次クールをsitemapに追加、診断の機械監視化（PR #113）の詳細記録は`docs/operations.md`の
㉟「㉞の診断が部分的に誤っていた件と、その訂正」に統合済み（「直近3世代だけ残す」の
運用ルールに従い、このファイルからは本文を削除）。

---

# 引き継ぎメモ（2026-08-24〜08-25・Vercel利用上限超過とその対応）は`docs/operations.md`の㉝を参照

Vercel Hobbyの利用上限超過による本番停止（HTTP 402・約6時間以上、08-24）と、その対応
（一次対応: `vercel.json`の新設・ISR再検証の統一・middlewareの往復解消／根本対応:
再検証をタグでの指名方式に切り替え・事前生成の拡大、いずれも08-25）の詳細記録は
`docs/operations.md`の㉝「Vercelの利用上限を超えてサイトが停止した」に統合済み
（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。
