# 引き継ぎメモ（2026-09-07・日次巡回・コード変更なし）

## 今日やったこと・分かったこと

- **`node scripts/seo-report.js`**（GSC最終日2026-09-03）: 直近28日クリック111・表示9716・
  CTR1.1%・平均順位24.5位（08-08時点20.6位から悪化）。①は表示急増（678→9716）に伴う
  希釈で、`docs/operations.md`㉟が既に説明した型と同じ（作品ページの表示が
  次クールsitemap分だけ増え、平均順位・CTRの分母が薄まる）。
  ③面別1ページあたりクリックは**声優1.90が引き続き最効率**（10ページ・19クリック・
  12.1位）。**作品は1.04まで改善**（08-19時点0.27・09-02時点0.83から連続で上昇、
  ページ数も37→79）。④の週次推移では08-17週(34/2881)・08-24週(35/3725)がどちらも
  完全な週で、表示が伸びる一方クリック数はほぼ横ばい＝ページが増えた分だけ広く
  浅く拾われている形。**この上昇は次クールsitemap追加（既存の承認済み方針）の
  副産物であり、`docs/seo-operations.md`2節が言う「量を増やす方向への投資」を
  新たに行った結果ではないため、同節の書き換えは見送った**（声優が依然トップな
  のでこの日の実測だけでは優先順位そのものは変わらない。数字の転記もしない）。
  ⑤(b)は`cloverworks/crunchyroll/engi アニメ一覧`が10〜13位に固まったまま
  （09-02時点から変化なし）。`久保さんは僕を許さない アマプラ`が10.4位・28表示・
  1クリックで略称施策（⑥）が初めて2クリック目を記録。
- **`node scripts/check.ts` / `npx tsc --noEmit` / `npm run check`**: 全件OK。
  新規の`⚠`なし（「descriptionの基準」作品2件超過は既知で変化なし、
  「シーズンページのHTML量」に⚠なし）。
- **`node scripts/freshness.js`**: 失敗0件・警告2件（GSC 08-23/08-28・サイト行動ログ
  08-25、いずれも既知の回収可能な欠測）。
- **本番/Annictへの外向き通信**: プロキシが403で拒否（`__agentproxy/status`で確認）。
  そのため現在クール（2026年秋）の実地HTML検証は今回も不可。代わりにローカル本番ビルドで
  過去クール（`/season/2024/summer`）を検証し、h1=1・作品リンク158件で正常。
  トップページ（`/`）はh1こそ1（`h1.brand`）だが作品カードが0件＝「見つかりません」表示。
  これは`ANNICT_TOKEN`未設定＋現在クールが未キャッシュのための想定内の結果で
  （過去クールはスナップショットから返るため無関係）、回帰ではない。
- **GitHub Actionsの失敗Issueが2件**（Issue #140 fetch-upcoming・#141 track-season、
  いずれも本日発生）。track-season側は同日中に別ランが成功し`content/coverage/`へ
  09-07分がコミット済み（1980390）。fetch-upcoming側はまだ09-07分のコミットが無い
  （1日2回のうち2回目待ちの可能性）。どちらも単発（前回の同種失敗はどちらも08-26の
  1回のみ）で連続してはいない。この環境からはAnnict/AniListへ出られないため
  これ以上の切り分けは不可。
- **サイト行動ログ**（`content/analytics/site/2026-09-07.json`）: `follow_click`が
  直近7日・30日とも**4件**（09-03時点は30日で0件）＝**Xフォロー導線のクリックが
  初めて動いた**。`officialClicks`（未提携）はnetflix/d_anime各5件が最多で、
  `docs/affiliate-setup.md`の優先順位（dアニメ最優先）と引き続き整合。
- **X**: Issue #139（09-07下書き）・#136（09-06下書き）ともにコメント無し、
  週次成長キットIssue #118も同様。フォロワー・インプレッションは今回も取得できず。
- **アフィリエイト**: `content/affiliate/programs.ts`は引き続き3サービス
  （ABEMA・Prime Video・Hulu）で変化なし。

## 気になった点（対応はユーザー判断）

`claude/daily-ops-*`系のPRが2件（#132=09-04分・#137=09-06分）、機能PR2件
（#130 モデル構成の分離・#138 CLS対策の調査記録）も未マージのまま滞留している。
CIはいずれもgreenの想定（今回は個別確認していない）。マージ／クローズはユーザー判断
（`docs/daily-ops.md`5節の方針どおりセッション側では判断しない）。

## 目標の現在地（2026-11-06まで残り60日）

- Xフォロワー: 取得できず
- アフィリエイト成果件数: 取得できず（`/admin/analytics`はこの環境から到達不能）
- 提携済みサービス数: 3（ABEMA・Prime Video・Hulu、不変）

---

# 引き継ぎメモ（2026-09-04・表示速度。「2秒以上」の本当の原因を本番実測で特定して直した）

## 依頼

利用者から「アニメ視聴ガイドの表示が2秒以上。**Vercelの無料枠内のまま**速くしてほしい」。
対象は**トップ／シーズン一覧**（他のページ種別は未検証）。追加条件が4つ:
①恒久的に無料枠を超えない ②2秒未満 ③既存システムに不具合を起こさない
④**クールが増えるたびに作品数が増えることを織り込む**。さらに「**メインはスマホユーザー**」。

## いちばん大事なこと（前回の見立ては外れていた）

利用者からPageSpeed InsightsのAPIキーをもらい、**本番を初めて実地で測れた**。結果、
㊴（09-03のPR）で削ったDOMノード・バイト数は体感の主因では**なかった**。

- LCPは**100%が element-render-delay**（TTFBは6〜16ms＝サーバーは速い）。
- LCP要素は`h1.brand`。つまり「一番上の見出しが出るまで」が遅い。
- 主コストは**スクリプト評価**。トップ **802ms** に対しシーズンページ 469ms。
  逆にHTMLのパースはトップ **8ms**・シーズンページ 50ms。
  **「HTMLを読む」より「JSで組み立て直す」ほうが桁違いに高い。**

原因は2つ。どちらも**画面を見ている限り絶対に気づけない**。

1. **トップページ "/" のサーバーHTMLが空だった。** `useSearchParams()`を呼ぶ
   `TopPageExplorer`があったため、Next.js 14がSuspense境界を丸ごとクライアント描画へ
   退避させ、本番HTMLは`<div class="wrap"></div>`だけ（h1が0個・作品リンク0件）。
   2026-08-05に`/season/**`は直したのに、**トップだけ「ディープリンクに必要」として
   残していた**。サイトの正面玄関＝canonicalの指す先が1ヶ月間、空だった。
2. **CSSが外部ファイルだった。** `<link rel="stylesheet">`のせいでHTMLが届いた後に
   もう1往復。ローカル実測で約**370ms**（PSIの見積もりは150ms）。

## やったこと（PR: `claude/anime-guide-performance-n0vvh2`・コミット3件）

詳細と数字は`docs/operations.md`の㊴（09-03分）と㊵（09-04分）。

1. **トップのサーバー描画を復旧**。`useSearchParams`を捨て、`TopPageExplorer`が
   **hydrationより前に**`window.location.search`から1度だけクエリを読む。
   `SeasonExplorer`は**初期描画では使わず**マウント後の`useEffect`で反映する
   （初期描画で使うとサーバーHTMLと食い違い、クエリ付きで開いた人にだけhydrationが壊れる）。
2. **CSSをHTMLに焼き込んだ**。`scripts/build-inline-css.js`が`app/globals.css`を
   `app/inlineCss.ts`へ変換し、`predev`/`prebuild`で自動再生成。ミニファイアは
   「壊れると全ページ無スタイル」なので賢い最適化をしない（コメント除去と空白畳みだけ）。
3. **サムネイルをWebPに**（110枚・4.11MB→2.51MB＝**−39%**、見た目は同じ）。
   `sharp`は依存に入れない（再生成時だけ`npm install --no-save sharp`）。
4. **逆戻り防止の検査を3節追加**（`scripts/check.ts`）＋`verify-production.sh`に
   **A0節**（トップのh1・作品リンク・`rel="stylesheet"`が無いこと・`<style>`があること）。
   6通りの壊し方で落ちることを確認済み。

## 効果（ローカル本番ビルド・5回の中央値・CPU4倍/1.6Mbps/390×844のスマホ相当）

| ページ | 変更前 LCP | 変更後 LCP |
|---|---|---|
| `/season/2025/summer`（173作品） | 992ms | **472ms（−52%）** |
| `/season/2010/spring`（93作品） | 824ms | **400ms（−51%）** |
| `/anime/14876` | － | 320ms |

トップは変更前の数字が取れない（**HTMLが空でLCP要素が存在しなかった**）。
h1は0個→1個、作品リンクは0件→今期の全作品に復帰した。

## ④「作品数が増える」への答え（推測ではなく実測）

- 作品1件あたり **1.0ms・約4.6KB**（93作品400ms／173作品472ms から導出）。
- 500作品でも**約810ms**の見込み＝2秒には当たらない。
- それでも気づけるように、`verify-production.sh`に**シーズンページHTML 2MB
  （約430作品）で落ちる**しきい値を入れた。

## 無料枠への影響

- **ISR Writesは1件も増えない**（`revalidate`もページ数も変えていない）。
- トップのSSRが復活したぶんのCPUは**月あたり約22秒＝4時間枠の0.15%**。
- 転送量はHTML +8KB／回だが、**CSSのリクエストが1本減り、画像は39%小さくなる**。

## 残っていること

- **本番での答え合わせ（PSI）が未実施。** この環境から本番URLへ出られないため、
  **マージ後に本番を測る**方針で利用者と合意済み（2026-09-04）。
  測る対象はトップ／`/season/2025/summer`／作品ページの3面と、CrUXの実ユーザー値。
- ㉝のチェックポイント（2026-09-18）で無料枠3指標の実測を確認する。

## 見送った案（理由つき。再提案の前に読むこと）

- **flightペイロードの圧縮**（services/castNames）… バッジ・アフィリエイトの経路
  （重大度高）を触るのに、HTML全体の7.5%しか減らない。
- **折りたたみパネルの遅延描画** … パネルはDOMの1.4%しかない。
- **`legacy-javascript`のbrowserslist絞り込み** … 12KiBのために対応ブラウザを狭める。
- **カード一覧のイベント委譲化** … 効果は最大だが「不具合を起こさない」条件下で
  最も壊すリスクが高い。

---

# 引き継ぎメモ（2026-09-03・日次巡回・コード変更なし）

## 今日やったこと

`node scripts/seo-report.js`（GSC最終日2026-08-30）・`node scripts/freshness.js`・
`npx tsc --noEmit`・`node scripts/check.ts`・`npm run build`（+ローカル`next start`でSSR実地確認）
を実行。新規の`⚠`・コードの不具合は見つからなかった（「descriptionの基準」の既知⚠2件は変化なし、
「シーズンページのHTML量」に⚠なし）。`freshness.js`も失敗0件（既知の欠測2件のみ）。

- 全体の推移（①）: 直近28日でクリック+85・表示+7316（678→7994）と伸びは続いているが、
  CTRは2.4%→1.3%、平均順位は20.6→22.7へ悪化。ただし**08-24週は「作品」「声優」とも
  `days:7・partial:false`＝完全な週**（PR #120がまだ未マージのためレポート表示には出ないが、
  生データを直接確認した）なので、悪化は途中週のノイズではない。`docs/operations.md`㉟が
  記録した「表示急増は次クールsitemap追加による長い裾の指標な希釈で、作品ページのクリック自体は
  落ちていない」という既存の診断と整合する範囲内であり、新しい異常ではないと判断した。
- ③面別効率: 声優2.22・作品0.90（37ページ0.27から改善）・制作会社が初めてクリックを持った
  （1件）。⚠表示ゼロの面は監督/シーズン/独占。
- 3節の判定表（監督・制作会社ページ／声優ページ過去クール拡張／略称・口語形の露出）は
  いずれも判定日が2026-09-11〜09-16でまだ先。⑤⑥に単発で手を入れられる明確な候補は無し
  （cloverworks/engiの「アニメ一覧」型クエリは既に`lib/pageMeta.ts`のtitleパターンと
  一致しており、追加の変更点が見当たらない）。
- Xの数字: Issue #128（09-03下書き）・#118（週次成長キット）ともにコメントなし、
  引き続き取得できず。site-analytics（09-03分）: `follow_click`は30日で0のまま、
  `affiliate_click`3件（prime2・abema1）、未提携`officialClicks`はd_anime最多（5件/30日）で
  既存の優先順位（`docs/affiliate-setup.md`）と整合。提携済みサービスは3（変化なし）。
  外向き通信はプロキシが本番・Annict双方へのCONNECTを403で拒否（`__agentproxy/status`の
  `recentRelayFailures`で確認）していたため、ローカルビルドでの検証に留めた。

## ⚠️ 未マージのPRが4件に増えている

`claude/daily-ops-*`系・機能PRとも、CIはすべてgreenだが未マージのまま滞留:
- #127 Annictキャッシュタグを作品単位へ（Active CPU削減の本命・Vercel利用上限対応の続き）
- #126 Active CPUのルート別実測（対策候補1は効果なしと判明・記録のみ）
- #124 日次巡回の記録（09-02・コード変更なし）
- #120 seo-report.jsに「途中の週」の印を表示する

いずれも`mergeable_state`は`clean`または`unknown`（コンフリクトなし）。特に#127は
Vercel利用上限（Issue #123・3倍枠の答え合わせ）に直結する対策なので優先度が高い。
マージ判断はユーザー側（このセッションは日次巡回のみで他PRの作成者ではないため、
確認のみで手を出していない）。

## 次回やること候補

1. 上記4件のPRのマージ（特に#127・#123のVercel答え合わせ）。
2. 3節の判定表（監督・制作会社／声優過去クール拡張）の判定日が2026-09-11〜12に迫っている。
   その週の`seo-report.js`実行時に判定すること。
3. Xアナリティクス・dアニメストア提携申請状況の確認（継続、今回も取得できず）。

---

# 引き継ぎメモ（2026-09-02・コード変更なし。Vercel利用量の答え合わせIssue #123起票）

要点のみ（「直近3世代だけ残す」運用ルールにより圧縮）。声優1ページあたり2.25で最効率
（作品0.83だが直近週は途中データ）。制作会社ページが初めて表示回数を持った。
サイト行動ログの実測が初めて付いた（`officialClicks`はd_anime最多）。
アフィリエイト3サービス・Xの数字は取得できず、外向き通信は403で不可。
詳細は該当コミット・Issue #123（Vercel利用量の答え合わせ）を参照。

---

# 引き継ぎメモ（2026-09-01・seo-report.jsの週次推移に「途中の週」の印を足した）は`docs/operations.md`の㉟を参照

GSCの3日ラグで最新週が必ず途中までしか埋まらない件に対し、`weeklyByType`の各行に
`days`/`partial`を持たせ、レポート表示にも`*`を出すようにした（PR #120）。詳細記録は
`docs/operations.md`の㉟「㉞の診断が部分的に誤っていた件と、その訂正」に統合済み
（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。

---

# 引き継ぎメモ（2026-08-31・声優ページの索引規則の作り直し・次クールsitemap追加）は`docs/operations.md`の㉟を参照

GSC実測に基づく声優ページの索引規則の作り直し（今年は全部／過去年は総出演50作品以上）、
次クールをsitemapに追加、診断の機械監視化（PR #113）の詳細記録は`docs/operations.md`の
㉟「㉞の診断が部分的に誤っていた件と、その訂正」に統合済み（「直近3世代だけ残す」の
運用ルールに従い、このファイルからは本文を削除）。

---

# 引き継ぎメモ（2026-08-24〜08-25・Vercel利用上限超過とその対応）は`docs/operations.md`の㉝を参照

Vercel Hobbyの利用上限超過による本番停止（HTTP 402・約6時間以上、08-24）と、その対応
（一次対応: `vercel.json`の新設・ISR再検証の統一・middlewareの往復解消／根本対応:
再検証をタグでの指名方式に切り替え・事前生成の拡大、いずれも08-25）の詳細記録は
`docs/operations.md`の㉝「Vercelの利用上限を超えてサイトが停止した」に統合済み
（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。
