# 引き継ぎメモ（2026-08-23・日次巡回）

## 今日やったこと（コード変更なし）

外向き通信は本番ドメイン・Annictとも今日も403（プロキシ遮断）。GitHub側のみMCP経由で到達できた。

- **`node scripts/seo-report.js`を先頭で実行**。GSC最終日は**2026-08-19のまま**（3日ラグの範囲内。
  `content/analytics/gsc/`に08-20〜08-22の取得コミットはあるが、いずれもラグにより中身は
  08-17〜08-19ぶんで、08-19が依然最新）。直近28日: クリック23・表示1051・CTR2.2%・
  平均順位19.0（前回08-17時点の21/802/20.2から改善）。
  ③面別効率は声優ページが1ページあたり5.00クリックで作品ページ0.11の傾向継続、
  新しいアクションは無し。
  ④週次推移で直近2週（08-10→08-17）だけ見ると**作品ページの表示が+148・声優ページが-14**と
  従来（`docs/seo-operations.md`2節）と逆方向の動きが出ているが、2週間未満の変動でもあり
  1週分のノイズの可能性が高い。方針は変えず、次回以降さらに追う。
  ⑤(b)の11〜20位クエリは「unanswered butterfly」「犬になったら好きな人に拾われた netflix」に
  入れ替わっており、08-19時点にあった「逃げ上手の若君 second期×サービス名」のクラスタは
  直近のクエリ上位100件から消えていた（1ページ目に上がった証拠は無く、放送終了に伴う検索需要の
  自然減とみられる。要出典なしの推測なので断定はしない）。
- `npm ci` → `npm run check`（tsc → check.ts → 周辺検査9本）**全件OK**。
  「シーズンページのHTML量」に`⚠`なし（最大クール2024-autumn・224作品・gzip推定79.4KB）。
  descriptionの幅検査で「作品」面に既存の`⚠`（1961件中2件が超過・削れない）が出ているが、
  この2件は元々作品名自体が長く削れない既知の許容ケースで新規の回帰ではない。
- **自前計測（site-analytics）に初めて実測らしい数字が出た**: `content/analytics/site/2026-08-22.json`
  （rowCount 21・30日窓）。`officialClicks`（未提携サービスへの公式サイトリンク）は
  netflix 2・disney 1・bandai 1・d_anime 1の**合計5件**。`affiliateClicks`は0件のまま。
  ただし母数がまだ極小（5件）で`docs/affiliate-setup.md`の優先順位（掲載本数ベース＝dアニメ最多）を
  実測ベースに置き換える判断はまだできない。もう数日〜1週間分溜めてから見る。
- **GitHub側の点検**: オープンPRは**#91のみ**（2026-08-21作成・`docs/handoff.md`の3世代圧縮。
  CI・Vercelとも成功、mainより2日遅れているが本ファイル以外は触っていないためコンフリクトの
  心配は無い）。**今回、#91のブランチを本ブランチにマージしたうえで本エントリを追記した**ため、
  今回のPRが#91の内容を包含する形になる（#91は今回のPRのマージ後、クローズしてよい）。
  オープンIssueは4件（#93/#92のX下書き、#90次クール準備、#76週次成長キット）。
  #90（2026年秋クールの準備チェックリスト）はAnnict照会が要る`audit-coverage.ts`から始まる
  手順で、`ANNICT_TOKEN`を要するためこの環境では実行不可（Issue本文もローカルPC作業として案内済み）。
- **Xの数字（目標1）**: Issue #93/#92/#76のコメントを確認したが、インプレッション・
  フォロワー数の記入は無し（今回も取得できず）。
- **アフィリエイト（目標2）**: `content/affiliate/programs.ts`は引き続き3サービス
  （ABEMA・Prime Video・Hulu）で変化なし。

## 今日の判断

GSCの傾向（声優優位）は継続、Xの数字は今回も未記入、アフィリエイト提携数も不変。
`npm run check`も全件OK・異常を示すIssue/PRも無し（#91は異常ではなく単に未マージ）。
CLAUDE.mdの「やることが無い日は無理に作り出さないこと」に従い、**コード変更は無し**。
今回はドキュメントのみ: `docs/handoff.md`を#91の圧縮を取り込んだうえで本エントリを追記。

## 次回やること候補

1. site-analyticsのサンプルが増えたら（`rowCount`・`officialClicks`合計が積み上がったら）
   未提携クリック順で`docs/affiliate-setup.md`の優先順位を実測ベースに更新する
2. ④の「直近2週だけ声優が落ち作品が伸びた」動きが継続するか、次回以降のGSC取得で確認する
   （2週間未満のノイズの可能性が高いため、方針変更は2週間分揃ってから）
3. Xアナリティクスの記入待ち（Issue #76/#93等のコメント。無ければユーザーに再度促す）
4. 次クール準備Issue #90のチェックリスト（`audit-coverage.ts`等）はローカルPC作業（`ANNICT_TOKEN`要）
   なのでユーザーに案内する

---

# 引き継ぎメモ（2026-08-21・日次巡回）

## 今日やったこと（コード変更なし）

外向き通信は本番ドメイン・Annictとも今日も403（プロキシ遮断。`$HTTPS_PROXY/__agentproxy/status`の
`recentRelayFailures`で確認）。GitHub側のみMCP経由で到達できた。

- **`node scripts/seo-report.js`を先頭で実行**（②の判断順どおり）。GSC最終日2026-08-17・直近28日:
  クリック21・表示802・CTR2.6%・平均順位20.2位（08-17時点の19/828/20.07から微増）。
  ③面別効率は声優ページが1ページあたり2.00クリックで作品ページ0.28の約7倍のまま、傾向継続
  （`docs/seo-operations.md`2節の重点方針から変更なし）。⑤(b)の11〜20位クエリ・⑥の略称クエリも
  既知の顔ぶれで新しい型は無し。**判断: 傾向に変化が無いためコード変更は無し**。
- `npm ci` → `npm run check`（9本）**全件OK**。「シーズンページのHTML量」に`⚠`も出ていない
  （最大クール2024-autumn・224作品・gzip推定79.4KB、目安282作品/100KBの範囲内）。
- **GitHub側の点検**: オープンPRは無し。オープンIssueは6件（#90次クール準備・#89/#88/#83のX下書き・
  #76週次成長キット）で、いずれも異常報告ではない。
  **Issue #84「サイト行動ログ集計の取得が失敗しました」（2026-08-19オープン）をclose**。
  `content/analytics/site/`に08-19〜08-21の3日分が連続でコミットされており（0a2d51b等）、
  初回セットアップ直後の一時的な失敗（同一run由来のコメント2件のみ）で以後は復旧済みと判断。
- **自前計測（site-analytics）の初回実データを確認**: `content/analytics/site/2026-08-21.json`は
  `rowCount:3`・`officialClicks`が bandai/netflix/d_anime 各1件のみ。**まだ「未提携クリックが
  多い順」を語れるサンプル数ではない**（30日窓で合計3件）ため、`docs/affiliate-setup.md`の
  優先順位（掲載本数ベース＝dアニメ最多）は今回は据え置き。affiliateClicksは0件。
- **Xの数字（目標1）**: Issue #76/#89/#88/#83のコメントを確認したが、インプレッション・
  フォロワー数の記入は無し（今回も取得できず）。
- **アフィリエイト（目標2）**: `content/affiliate/programs.ts`は引き続き3サービス
  （ABEMA・Prime Video・Hulu）で変化なし。dアニメの申請状況もユーザー作業のまま未確認。
- `docs/handoff.md`を5節の3世代ルールに沿って整理（2026-08-16以前を圧縮し参照に置き換え）。

## 次回やること候補

1. site-analyticsのサンプルが十分溜まったら（`rowCount`が増えたら）未提携クリック順で
   `docs/affiliate-setup.md`の優先順位を実測ベースに更新する
2. dアニメストアの提携申請状況の確認（ユーザー作業・`docs/affiliate-setup.md`）
3. Xアナリティクスの記入待ち（Issue #76/#89等のコメント。無ければユーザーに再度促す）

---

# 引き継ぎメモ（2026-08-19・利用者からの4つの問い合わせ）

## 何を訊かれたか

利用者から4点。①Xアナリティクスを取れないか ②Vercelのデータを自動で取れないか
③`/admin/analytics`を取得してほしい（無理なら手順） ④dアニメの別ASPをまとめてほしい
（**ドコモアフィリエイトにdアニメ終了と記載があった**との情報つき）。

## 到達性の実測（推測ではなく実際に叩いた）

`curl` ＋ `$HTTPS_PROXY/__agentproxy/status` で確認。ゲートウェイが **CONNECTに403** を返す:

| 宛先 | 結果 |
|---|---|
| `animedia-khaki.vercel.app`（本番） | 403 connect_rejected |
| プレビューデプロイ（`animedia-git-cla-...vercel.app`） | 403 connect_rejected |
| `api.annict.com` | 403 |
| `analytics.x.com` | 403 |
| `api.vercel.com` | 403 |
| `affiliate.docomo.ne.jp` | 403 |
| **WebSearch（検索）** | **使える** |
| WebFetch（任意ドメインの取得） | 遮断（`EGRESS_BLOCKED`） |

**この環境では①②③のいずれも「その場で取得」はできない**。ただし③は恒久的に解決できるので
下記のとおり実装した。①はX API無料枠廃止（2026-02）＋ログイン必須で、スクレイピングは
規約違反のため**実装しない方針**。②も同様にトークンが要る（リポジトリ内に無い）。

## やったこと（コミット `c8cfe3b`・PR #82）

### 1. 自前計測を機械から読めるようにした（③の恒久対策）

`/admin/analytics` の集計は**画面にしか無く、ブラウザで開く以外に読む方法が無かった**。
そのためセッションは実測値を一度も読めておらず、`docs/affiliate-setup.md`の
「未提携サービスへのクリックが多い順に提携する」＝**提携順を決める一次情報**が、
利用者の手入力待ちで止まっていた。GSCとまったく同じ形に載せた。

| 追加物 | 役割 |
|---|---|
| `lib/adminAnalytics.ts` | 集計を1箇所に集約。**画面とJSON窓口が同じ集計を通る** |
| `app/api/admin/analytics/route.ts` | 運営者専用JSON窓口。`x-admin-token`ヘッダー可・不一致は404・noindex |
| `scripts/fetch-site-analytics.js` | 日次取得。429/5xx再試行・404即失敗・未設定は静かにスキップ |
| `scripts/check-site-analytics.js` | 回帰テスト28件（HTTPスタブ・ネットワーク不要） |
| `.github/workflows/site-analytics.yml` | 毎日07:10 JST前後。失敗はIssueへ |

**外してはいけない点**: ①集計を画面かJSONに書き戻さない（数字が2通りになる）
②トークンはURLではなくヘッダー（ログ・リファラに残さない）
③`content/analytics/`は**コミット＝公開**されるので、書き出す直前に`assertNoSecrets`で
トークン混入を検査し**混じっていたら書かずに落とす** ④未設定なら静かにスキップ
（ここで失敗させるとセットアップが済むまで毎日Issueが積み上がる）。

**実地検査**（サンドボックスでも`localhost`は遮断されないので本番ビルドを起動して確認）:
トークン無し→404 / 不一致→404 / ヘッダー→200 / クエリ→200 / `X-Robots-Tag: noindex`。
さらに`fetch-site-analytics.js`を**実サーバー相手**に走らせ、サイト側未設定時に
ファイルを作らず成功終了することも確認した（スタブだけで済ませていない）。

**まだ動かない**。有効化には利用者側の3手順が要る（下記「ユーザー作業」）。

### 2. dアニメストアのASP情報を訂正（利用者の指摘が正しかった）

ドコモ アフィリエイト公式のお知らせ（`affiliate.docomo.ne.jp/news/view/374`）により、
**dアニメストアのプログラムは商流移管に伴い2026-03-31で終了**。新規募集は既に停止、
2026-04-01以降リンクはデッドリンク、最終承認5月・最終支払6月。**Leminoは継続**。

つまり`docs/affiliate-setup.md`が2026-08-11に書いた「ドコモアフィリエイトが本家で最優先」
「管理画面の『キャンペーン』から申請」は**当時すでに誤り**だった。
「メニューが紹介用だから出ない」という当時の説明も成り立たない（3月末で終わっていたため）。
訂正のうえ、申請先を **afb / A8.net / アクセストレード**（3社とも**登録済みASP**＝
新規のASP登録は不要）に差し替えた。報酬は405〜509円・afbが最高という記述が多いが
**すべて二次情報なので管理画面の表示が正**。

これにより「新しいASPに登録しないと在庫が増えない」という前提が変わり、
**いまの管理画面から申請するだけでdアニメ（掲載521作品・単独で+11.8%）に手が届く**。

## 検証

`npm run check` 全件OK（新テストを含め8本がCIと一致）／`rm -rf .next && npm run build` 成功／
CI（PR #82・`c8cfe3b`）は **ubuntu・windows・Vercel すべて success**。

## ユーザー作業（これが済むまで自前計測は動かない）

1. Supabase の SQL Editor で `analytics_events` テーブルを作る（DDLは`docs/operations.md`
   「計測の見かた」。RLSポリシーは意図的に1つも作らない）
2. **Vercel の環境変数**に `ADMIN_DASHBOARD_TOKEN` を追加
3. **GitHub Secrets** に**同じ値**で `ADMIN_DASHBOARD_TOKEN` を追加
   ← 食い違うと毎日404でIssueが立つ。ここが最も間違えやすい

済めば翌朝から`content/analytics/site/<日付>.json`が自動コミットされ、**以後セッションが毎日読める**。

## 次回やること候補

1. `content/analytics/site/` に最初のファイルが出ているか確認 →
   出ていれば**未提携サービスへのクリックが多い順**を読み、`docs/affiliate-setup.md`の
   「提携の優先順位」を掲載本数ベースから**実測ベース**に置き換える
2. dアニメストアを afb / A8.net / アクセストレード に申請した結果の確認（報酬額つきで
   連絡があれば`programs.ts`に登録。採用は`pickAffiliate`が自動で最高額に切替）
3. Xアナリティクスの記入待ち（Issue #76/#81）。到達不能は今回確定したので、
   代替として`follow_click`（上記1が動けば自動で読める）を見る
4. GSCが2〜3回分溜まったら、制作会社・監督ページ（8/12公開）の`weeklyByType`を答え合わせ
5. 次クール準備の窓は8/21開始（`season-prep.yml`が自動起票）

---

# 引き継ぎメモ（2026-08-18・日次巡回）

## 今日やったこと（コード変更なし）

外向き通信はこの日も本番ドメイン・Annict・Bluesky・Mastodonがすべて403（プロキシ遮断）。
GitHub APIのみ到達できた（MCP経由）。`.env.local`も無いため現在クール（2026年夏）の
`/api/season`実測は今回も不可。

- `npm ci` → `npx tsc --noEmit` → `npm run check`（`check.ts`＋6本の周辺検査すべて）を実行し
  **全件OK**。「シーズンページのHTML量」に`⚠`も出ていない（最大クール2024-autumn・224作品・
  gzip推定79.4KB、目安282作品/100KBの範囲内）。
- **GitHub側の点検**: オープンPRは無し（前回メモにあった心配事項はすべて解消済み）。
  ラベル`production-check`・`sns-failure`のオープンIssueも無し＝本番SSR検査・SNS自動投稿は
  直近失敗なし。main先頭（`0e907c7`）は`track-season`の自動コミットで、GitHub Actions
  14本は今日も正常に回っている様子（Issue経由の異常報告が無いことから判断。ワークフロー実行
  一覧はレスポンスが大きすぎて今回は個別取得を断念）。
- **GSC実測**（`content/analytics/gsc/2026-08-14.json`。期間2026-07-18〜08-14の28日累計）:
  クリック**20**・表示回数**842**・平均掲載順位**19.86**。前回（08-13時点：19/828/20.07）から
  ほぼ横ばい。`weeklyByType`も既知の傾向のまま（声優ページのCTR・順位が作品ページより優位）で、
  直近週（08-10週）は声優CTRが15.6%→5.4%に落ちたが表示数29→37と小さく、1週のノイズと判断し
  様子見。**制作会社・監督ページ（8/12公開）はこの時点でもまだ`weeklyByType`に現れず**
  （GSCの3日ラグ＋公開から日が浅いため。判定は次回以降に持ち越し）。
  → **傾向に変化が無いため新しいアクションは無し。**
- **Xの数字（目標1）**: Issue #81（08-18下書き）・#76（週次成長キット）・#74（08-17下書き）を
  確認。ユーザーコメントはいずれも「旧フォーマットの本文をシャドウバン対応版に差し替えた」旨のみで、
  インプレッション・フォロワー数の記入は無し（今回も取得できず）。
- **アフィリエイト（目標2）**: `content/affiliate/programs.ts`は引き続き3サービス
  （ABEMA・Prime Video・Hulu）で変化なし。dアニメストアの申請状況もユーザー作業のまま未確認。
- **次クール準備の窓**: `scripts/lib/build-season-prep.js`の窓は8月21日開始（`WINDOW_START_DAY`）。
  今日は8/18で3日前＝まだ窓の外（自動化済みなので手を動かす必要なし、`season-prep`Issueも未起票）。

## 今日の判断

実測に動きが無く（GSCほぼ横ばい・X数字は今回も未記入・アフィリエイト提携数も不変）、
`npm run check`も全件OK・オープンな異常Issue/PRも無し。CLAUDE.mdの「やることが無い日は
無理に作り出さないこと」に従い、**コード変更は無し**。次に動くべきタイミングは
①GSCが1〜2週分ノイズを均して監督・制作会社ページの効果が見え始めたとき、
②Xアナリティクスの数字が記入されたとき、③9月中旬（配信サービス特設ページの窓）。

## 次回やること候補

1. Xアナリティクスの記入待ち（Issue #76/#81のコメント。無ければユーザーに再度促す）
2. dアニメストアの提携申請状況の確認（`docs/affiliate-setup.md`）
3. GSCが2〜3回分溜まったら、制作会社・監督ページ（8/12公開）の`weeklyByType`を答え合わせする
4. `docs/operations.md`「実測サマリ」表への追記（Vercel Analytics・Xの数字が揃ってからまとめて）

---

# 引き継ぎメモ（2026-08-17・日次巡回）

## 今日やったこと（コード変更なし）

外向き通信はこの日も本番ドメイン・Annict・SNS APIすべて403（プロキシ遮断）。ローカルの
`npm run check`系のみ実施し、実行できたことと実行できなかったことを分けて記録する。

- `node scripts/check.ts` 全件OK。「シーズンページのHTML量」に`⚠`も出ていない
  （最大クール2024-autumn・224作品・gzip推定79.4KB。目安282作品/100KBの範囲内）。
  現在クール（2026年夏）はライブ取得が要るため今回も未確認（`ANNICT_TOKEN`が環境に無い）。
- **GSC実測**（`content/analytics/gsc/2026-08-13.json`。GSCは取得に約3日ラグがあるため
  実質2026-08-10前後までのデータ。期間は2026-07-17〜08-13の28日累計）:
  クリック**19**（7/26実測の7から増）・表示回数**828**（同240から増）・
  平均掲載順位**20.07**（19.8からほぼ横ばい）。`docs/operations.md`「実測サマリ」表に未反映
  （Vercel Analytics・Xの数字が今回も取得できず行を埋めきれないため、次回まとめて追記する）。
  ㉗の「声優ページがCTR・順位の両方で作品ページより優位」という傾向は週次データ
  （`weeklyByType`の2026-08-10週）でも継続（声優CTR15.6%・平均5.3位 vs 作品CTR1.1%・平均21.4位）。
  **08-15に結論済みの内容と変わらないため新しいアクションは無し**。
- 制作会社・監督ページ（8/12公開）は`weeklyByType`にまだ「監督」「制作会社」の行が現れない
  （公開直後＋GSCの3日ラグのため）。効果判定は次回以降のGSC取得を待つ。
- **Issue #75「SNS自動投稿が失敗しています」はclose済み。** 原因はBlueskyの一時失敗（evening枠、
  同じ枠でThreadsは成功）。当初このメモは「1つでも投稿できれば枠ごと投稿済みにする設計どおりの
  挙動で不具合ではない」と書いていたが、**それは誤りで、Blueskyのその日の枠が永久に落ちる不具合
  だった**（印をBluesky/Threadsで共有していたため、片方成功で枠が閉じ、失敗した側が再試行されない）。
  2026-08-17に**PR #78で修正済み**＝印を「投稿先ごと」と「枠ぜんぶ」の2段に分け、部分失敗した
  投稿先だけを再試行する（全成功でだけ二重投稿を防ぐ。経緯は`docs/operations.md`の㉚）。
  Issue本文の「次の起動が自動で再試行」という案内も実態に合わせて修正済み。
- Issue #49（8/10の週次X成長キット）はIssue #76に代替わりしたのでclose。

## 未処理と書いていたPRは同日中に全てマージ済み（2026-08-17に追記訂正）

このメモ作成時点で「未マージ」と書いたPRは、同じ2026-08-17中にすべて解決した:

- **PR #73**（作品ページだけ配信情報なしになる不具合＝`Program.episode`要求でprogramノードが
  null伝播で消える）→ マージ済み（このメモ作成の直前13:46）。作成直後のため気づけていなかった。
- **PR #64**（seriesList調査の記録）→ mainが進んでいたのでrebaseではなく**merge方式**で衝突を
  解決してマージ済み（seriesList実測は`docs/operations.md`の㉒配下のサブ節へ移設）。
- **PR #78**（上のIssue #75の部分失敗マーカー不具合の修正）→ マージ済み（㉚）。
- **PR #79**（⑬流入リード発掘を凍結し、効果測定を⑨AI引用モニタリングへ一本化）→ マージ済み。

## Xの数字（目標1）
週次Issue #76・失敗通知#75・下書き#74のコメントを確認したが、ユーザーからの
インプレッション・プロフィールアクセス・フォロワー数の記入は無し（今回も取得できず）。

## アフィリエイト（目標2）
`content/affiliate/programs.ts`は引き続き3サービス（ABEMA・Prime Video・Hulu）。
`/admin/analytics`はログインが要るためこの環境からは見られず、成果件数・
`official_link_click`のサービス別内訳は今回も取得できず。

## 次回やること候補
1. Xアナリティクスの記入待ち（Issue #76のコメント。無ければユーザーに再度促す）
2. dアニメストアの提携申請状況の確認（`docs/affiliate-setup.md`）
3. `docs/operations.md`「実測サマリ」表に今回のGSC実測値（クリック19・表示828・順位20.07）を追記する
   （Vercel Analytics・Xの数字が揃ってからまとめて追記でもよい）

---

## それ以前のアーカイブ（2026-08-21に3世代ルールで圧縮）

2026-08-16〜2026-08-06の引き継ぎメモ本文は、5節の方針（直近3世代だけ残し、それ以前は
`docs/operations.md`の該当番号への参照に置き換える）に沿ってここから削除した。全文はgit履歴
（このコミットの直前）に残っている。要点と参照先:

- **2026-08-16（運用の棚卸）**: X運用をシャドウバン対応に組み直し（頻度1日1回以下・本文にリンクを
  入れない・タグを減らす・本文を毎回変える）→ `docs/operations.md`の㉘。GitHub Actions 14本の健全性を
  確認し、手順書（`docs/operations.md`）の破損3件を修正。手動運用（流入リード発掘・AI引用モニタリング）
  が止まっていることを確認（判断は継続保留）。
- **2026-08-15（8日分の日次巡回まとめ）**: 声優データの取りこぼし修正・スナップショット再生成、
  GSC日次取得の稼働開始と「声優ページ優位」の発見 → `docs/operations.md`の㉗、次クール網羅調査
  → `docs/next-season-coverage.md`、Discord `/anime`コマンドの不具合修正、Annict `seriesList`調査
  → `docs/annict-serieslist-probe.md`。
- **2026-08-07（×2）**: Xアカウントのシャドウバン判明・投稿を約1週間停止する判断（`docs/operations.md`
  の㉘に帰結）。本番SSR実地検査を`scripts/verify-production.sh`＋GitHub Actionsへ自動化（3セッション
  持ち越しの決着。CLAUDE.mdの該当節・`docs/operations.md`の⑦-10参照）。
- **2026-08-06（×3）**: SSRページが空HTMLを返していた不具合の修正（`components/TopPageExplorer.tsx`
  新設。`docs/operations.md`の⑦-10）、過去クールをsitemapに開放（⑦-11）、埋め込みウィジェット・
  公開APIを新設し被リンク獲得策へ転換（⑯）、放送終了作品への「配信中」断定を是正（⑰）、
  Xフォロワー100人・アフィリエイト10件（期限2026-11-06）の目標を設定（`docs/daily-ops.md`に集約済み）。

---

