# 引き継ぎメモ（2026-09-25・コード変更なし。不具合・滞留なし）

## 今日やったこと

`CLAUDE.md`→`docs/daily-ops.md`→`docs/seo-operations.md`→`docs/handoff.md`の順で読み、
`node scripts/seo-report.js`（GSC最終日2026-09-22）・`usage-report.js`（Hobbyのため明細0件＝想定通り）・
`freshness.js`（失敗0・警告2＝GSC欠測4日分とサイト行動ログ欠測1日分だがいずれも許容日数内で
「収集が止まっている」には該当せず・既知の欠測21件）・`npm ci && npm run check`（全件OK）・
`rm -rf .next && npm run build`（4,984ページ）・`check-page-css.js`（全OK）・`check-build-size.js`
（490.5MB/予算1083.3MB=45%・見通し10.0年）・ローカル`next start`でのSSR確認
（`/season/2024/summer` h1=1/作品リンク159件、`/` h1=1）を実行。**不具合は見つからず、コードの
変更は行っていない。**

open PRは0件・CI（`ci.yml`）は直近まで全件success。open issueはXの日次下書き（#178・#175、
運用どおり）・週次X成長キット（#168）・次クール準備（#90、後述）のみで、滞留や誤報は無かった。

## 目標の現在地

- **Xフォロワー**（目標100）: **24人**（Issue #168の2026-09-23コメント。前回確認20人から+4）。
  同投稿の実測: インプレッションは数十件出ているがプロフィールアクセスは2件とも0
  （`docs/daily-ops.md`2.5(b)の切り分けで「表示は出ているがプロフィールへ来ていない」型）
- **アフィリエイト成果**（目標10件）: 掲載は2026-09-09の判断により恒久停止中（変化なし）
- 提携済みサービス: 3（ABEMA・Prime Video・Hulu）。**全リンクactive:false**のため実質稼働なし
- 2026-11-06まで残り**42日**

## SEO（`seo-report.js`・GSC最終日2026-09-22）

直近28日: クリック+198・表示+12790（ローリング、順位は悪化4.0）。②で前半68/後半146クリックと
確認済み＝実数としても伸びている。③面別効率は**サービス別5.13**（8ページ・41クリック・
CTR12.6%）が最上位、声優2.88・ランキング5.00（1ページのみ）・作品1.25。⑤(b)最大の塊は
「cloverworks アニメ 一覧」（81表示10.7位、継続）、次点「彼方から アニメ 配信」（55表示10.5位）。
判定表（`docs/seo-operations.md`3節）の期日は2026-10-12でまだ先、今回も判定なし。

## 次クール準備（Issue #90・2026年秋）

チェックリストは未着手のまま（`audit-coverage.ts`の実行に`ANNICT_TOKEN`とAnnictへの外向き
通信が要るが、**この作業環境は`.env.local`が無くAnnictへも出られない**ため実行不可。ローカルPC
作業が必要）。窓（クール開始の約1〜1.5ヶ月前）自体はまだ開いているので急ぎではない。

## 未マージPR

なし。

## 明日以降やること候補

1. `docs/seo-operations.md`3節の判定表は2026-10-12まで動きなし。
2. サービス別面の効率がさらに浮上（4.00→5.13）。ページ数はまだ8件と少なく結論は時期尚早だが、
   「勝っている面を厚くする」の最有力候補として次回以降も③で追う。
3. Issue #90（次クール準備）はローカルPC作業待ち（`ANNICT_TOKEN`要・`audit-coverage.ts`実行）。
4. Xアナリティクス（Issue #168）・dアニメストア提携申請状況は引き続き画面の目視が必要な部分あり。

---

# 引き継ぎメモ（2026-09-23・コード変更なし。Issue #146の誤報とPR#170の重複を清算した）

## 今日やったこと

`CLAUDE.md`→`docs/daily-ops.md`→`docs/seo-operations.md`→`docs/handoff.md`の順で読み、
`node scripts/seo-report.js`・`usage-report.js`・`freshness.js`・`npm ci && npm run check`
（全件OK）・`rm -rf .next && npm run build`（4,980ページ）・`check-page-css.js`（全OK）・
`check-build-size.js`（509.9MB/予算1083.3MB=47%。前日比で予算が650→1083MBに広がっているのは
PR #171の保持期間見直しの効果）・ローカル`next start`での`/season/2024/summer`確認
（h1=1・作品リンク159件）を実行。コードの不具合は見つからず、変更は行っていない。

**見つけて片付けたのは、前日までの巡回が積み残した重複・誤報3件。**

1. **PR #170（昨日の巡回が作成・未マージ）は、対象のバグが既にmainで別実装により
   解消済みだった。** #170はIssue #146（本番SSR実地検査が9/9〜9/21の13日間失敗）を
   直そうとしていたが、**同じ夜（2026-09-22 23:51）にマージされたPR #171の3コミット目が
   別のアプローチ（`wantnot`/`want`の向きを反転＋C節に対の検査）で同じ症状を先に
   修正していた**。09-22 22:55の定期実行（`verify-production.yml` run #47）が
   mainのコミット`09efcaf`で成功していることを確認し、実測で裏取りしてから
   #170はcloseした（説明コメント付き。ブランチはmainから3日分遅れておりCI設定・
   `check-build-size.js`など広範囲でコンフリクトする状態だった）。
2. **Issue #146自体も上記の理由でclose**（run #47成功を根拠に記載）。
3. **Issue #141・#140（AniList側403による欠測、2026-09-07〜09-12）も自然復旧済みで
   放置されていた。** track-season.ywは直近43回・fetch-upcoming.ymlは直近3回とも
   成功していることを確認してからclose。`scripts/freshness.js`のACKNOWLEDGEDに
   既に記録済みの既知欠測なので、これ以上追う必要は無い。

Issue #90（次クール準備・2026年秋）とIssue #158（Vercel利用量ダッシュボードの答え合わせ）は
引き続き open（前者はまだ窓の作業途中、後者はユーザーのダッシュボード目視待ち）。

## 目標の現在地

- **Xフォロワー**（目標100）: 取得できず（Issue #157以降の新しいコメントなし。前回確認20人）
- **アフィリエイト成果**（目標10件）: 掲載は2026-09-09の判断により恒久停止中（変化なし）
- 提携済みサービス: 3（ABEMA・Prime Video・Hulu）。**全リンクactive:false**のため実質稼働なし
- 2026-11-06まで残り**44日**

## SEO（`seo-report.js`・GSC最終日2026-09-19）

直近28日: クリック+155・表示+12590（ローリング）。平均順位は悪化基調のまま（24.5→25.3付近で
横ばい〜微悪化）。③面別効率は**サービス別4.00**（6ページ・24クリック・CTR20.5%）が最上位、
声優2.00・トップ2.00・作品1.19。⑤(b)最大は「cloverworks アニメ 一覧」（86表示10.7位）、
次点「彼方から アニメ 配信」（32表示10.3位）。判定表（`docs/seo-operations.md`3節）の期日は
2026-10-12でまだ先、今回は判定なし。

## 未マージPR

なし（#170はclose済み）。

## 明日以降やること候補

1. `docs/seo-operations.md`3節の判定表は2026-10-12まで動きなし。
2. サービス別面の効率が浮上している（3.40→4.00）。声優ページと同様「勝っている面を厚くする」
   の候補になりうるが、ページ数がまだ6件と少なく結論は時期尚早。次回以降も③で追う。
3. Xアナリティクス（インプレッション・プロフィールアクセス）・dアニメストア提携申請状況は
   引き続き取得できず（画面の目視が必要）。Issue #158（Vercel利用量ダッシュボード）も同様。

---

# 引き継ぎメモ（2026-09-21・㊷「同日対応」の誤りを訂正。コードの不具合は見つからず）

## 今日やったこと

`node scripts/seo-report.js`（GSC最終日2026-09-17）・`usage-report.js`・`freshness.js`・
`npm run check`（全件OK）・`rm -rf .next && npm run build`（4,980ページ）・
`check-build-size.js`（509.9MB/650MB=78%）・`check-page-css.js`（全OK）・
ローカル`next start`での本番相当HTML確認（`/season/2024/summer` h1=1/作品リンク159件、
`/anime/13180` 過去クールで「配信情報の取得日」を出さないことを確認＝PR #165の前提どおり）
を実行。コードの不具合は見つからなかった。

**見つけて直したのは記録の誤り。** `docs/operations.md`㊷は「Vercelから回答を受けた
2026-09-09に掲出を止めた（対応・同日）」と書いていたが、コミット履歴を確認すると
`active: false`化・プライバシーページの書き換え・㊷の本文自体が**すべてPR #160
（2026-09-19）で初めて入っていた**。つまり回答から実装まで**10日間**、3サービス4リンクは
`active: true`のまま稼働していた（ちょうど㊿の日次巡回停止期間と重なる）。
site-analyticsの`affiliateClicks`（Prime Video）が09-12頃・09-16頃に増え、停止後
（09-19〜09-21）は動いていないことから、この2件は「規約違反と分かっていながら
掲出を続けていた期間」のクリックと裏付けられる。対応は既に完了しているため追加実装は無く、
`docs/operations.md`㊷に訂正節を追加し、`content/affiliate/programs.ts`のコメントと
`CLAUDE.md`の該当箇所を実際の日付に合わせた。

## 目標の現在地

- **Xフォロワー**（目標100）: **20人**（Issue #157のコメント、2026-09-19時点）。
  07-26時点0人からは前進。site-analyticsの`follow_click`も30日間0だったのが
  直近30日で4件に増えている（09-14以降）
- **アフィリエイト成果**（目標10件）: 掲載自体が2026-09-09の判断により恒久停止中
  （実装09-19。上記参照）。成果件数は取得手段が無い（ASP管理画面はログイン制）
- 提携済みサービス: 3（ABEMA・Prime Video・Hulu）。**全リンクactive:false**のため実質稼働なし
- 2026-11-06まで残り**46日**

## SEO（`seo-report.js`・GSC最終日2026-09-17）

直近28日: クリック+146・表示+13013（28日ローリング）。平均順位は悪化基調が続く
（19.0→25.4）。③面別効率はサービス別3.40・声優3.00・作品1.16。⑤(a)表示20回以上で
クリック0のページは無し。⑤(b)最大の塊は「cloverworks アニメ 一覧」（81表示10.6位）。
判定表（`docs/seo-operations.md`3節）の期日は2026-10-12でまだ先、今回は判定なし。

## 未マージPR

- **#165**（2026-09-20作成・`verify-production.sh`の誤検知を直す）が未マージのまま。
  CI状況・マージ判断はユーザー側

## 明日以降やること候補

1. #165のマージ判断（ユーザー側）。
2. 3節の判定表は2026-10-12まで動きなし。
3. Xアナリティクス（インプレッション・プロフィールアクセス）・dアニメストア提携申請状況は
   引き続き取得できず（画面の目視が必要）。

---

# 引き継ぎメモ（2026-09-15・日次巡回が7日止まっていた件の発見と、その間に紛れ込んだ本番不具合の修正）は`docs/operations.md`の㊾・㊿を参照

日次巡回が2026-09-08〜09-14の7日間動いていなかった件（原因はリポジトリ外＝特定不能。
`docs/operations.md`㊿）と、公開API・埋め込みが400を返すべき場面で404を返していた不具合
（PR #138のmiddleware変更が原因・6日間・Issue #146。`docs/operations.md`㊾）の詳細記録は
統合済み（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。

---

# 引き継ぎメモ（2026-09-04・表示速度。「2秒以上」の本当の原因を本番実測で特定して直した）は
`docs/operations.md`の㊴・㊵を参照

トップページ"/"のサーバーHTMLが空だった件（`useSearchParams`起因）とCSSが外部ファイルだった件
（往復+370ms）を直し、LCPを`/season/2025/summer`で992ms→472ms（−52%）にした詳細記録は
`docs/operations.md`の㊴（09-03分）・㊵（09-04分）に統合済み（「直近3世代だけ残す」の運用
ルールに従い、このファイルからは本文を削除）。

---

# 引き継ぎメモ（2026-09-03・日次巡回・コード変更なし）は統合済み

（「直近3世代だけ残す」の運用ルールに従い本文を圧縮）。要点: SEOは伸びていたが平均順位は
悪化基調（20.6→22.7）で新しい異常ではないと判断。未マージPR#127・#126・#124・#120が
滞留していたが、いずれもその後マージ済み（09-19までに解消。`git log`で確認可能）。
提携済みアフィリエイトは3サービス（変化なし、後の09-19に全停止）。
数字は`content/analytics/gsc/`の当時のJSONを参照（転記しない方針）。

---

# 引き継ぎメモ（2026-09-02・コード変更なし。Vercel利用量の答え合わせIssue #123待ちを記録した）は
統合済み（「直近3世代だけ残す」の運用ルールに従い本文を圧縮）。要点: Issue #123は
ユーザーのVercelダッシュボード確認待ちのまま。提携済みアフィリエイトは3サービス
（ABEMA・Prime Video・Hulu）。数字はコード変更を伴わないため`docs/operations.md`への
対応する番号は無い（GSC断面は転記しない方針＝`content/analytics/gsc/`の当時のJSONを参照）。

---

# 引き継ぎメモ（2026-09-01・seo-report.jsの週次推移に「途中の週」の印を足した）は`docs/operations.md`の㉟を参照

GSCの3日ラグで最新週が必ず途中までしか埋まらない件に対し、`weeklyByType`の各行に
`days`/`partial`を持たせ、レポート表示にも`*`を出すようにした（PR #120）。詳細記録は
`docs/operations.md`の㉟「㉞の診断が部分的に誤っていた件と、その訂正」に統合済み
（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。

---

# 引き継ぎメモ（2026-08-31・声優ページの索引規則の作り直し・次クールsitemap追加）は`docs/operations.md`の㉟を参照

GSC実測に基づく声優ページの索引規則の作り直し（今年は全部／過去年は総出演50作品以上）、
次クールをsitemapに追加、診断の機械監視化（PR #113）の詳細記録は`docs/operations.md`の
㉟「㉞の診断が部分的に誤っていた件と、その訂正」に統合済み（「直近3世代だけ残す」の
運用ルールに従い、このファイルからは本文を削除）。

---

# 引き継ぎメモ（2026-08-24〜08-25・Vercel利用上限超過とその対応）は`docs/operations.md`の㉝を参照

Vercel Hobbyの利用上限超過による本番停止（HTTP 402・約6時間以上、08-24）と、その対応
（一次対応: `vercel.json`の新設・ISR再検証の統一・middlewareの往復解消／根本対応:
再検証をタグでの指名方式に切り替え・事前生成の拡大、いずれも08-25）の詳細記録は
`docs/operations.md`の㉝「Vercelの利用上限を超えてサイトが停止した」に統合済み
（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。
