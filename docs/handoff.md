# 引き継ぎメモ（2026-09-02・コード変更なし。Vercel利用量の答え合わせIssueに注意）

## ⚠️ 最優先: Issue #123（Vercel利用量の答え合わせ・判定日2026-09-02）はユーザー確認待ち

`docs/operations.md`㉝の対策デプロイ（2026-08-26）から1週間の節目で、
`usage-check.yml`が本日Issue #123を起票済み（未コメント）。Vercelダッシュボードは
ログインが要るためセッションからは絶対に読めない。**「1日あたりISR Writes 2,500件未満」
などの合格ラインをユーザーがダッシュボードで確認し、Issueにコメントする必要がある**。
3倍枠は2026-09下旬に切れるので、次の判定日（2026-09-18・最終確認）までに対応が要る。
合格していればコメント後に`docs/operations.md`㉝へ実測を追記するのが次の作業。

## 今日やったこと・分かったこと（コード変更なし）

- **ブランチ**: 実行環境が割り当てたのは`cla/wizardly-knuth-uul4tg`で、これは
  push直前に`origin/main`（46da576）と同一コミットだった。08-24と同じ事情
  （下記の08-24エントリ参照）なので、そのままこのブランチで作業した。
- **`node scripts/seo-report.js`**（GSC最終日2026-08-29）: 直近28日クリック94
  （08-08時点16から+78）・表示7542（+6864）・CTR1.2%（2.4%から低下）・平均順位22.6位
  （20.6から悪化）。③面別1ページあたりクリックは声優2.25が引き続き最効率で、
  **制作会社が初めて表示回数を持った**（1ページ7表示1クリック・8.7位）。3節の判定表
  「監督ページ・制作会社ページ」（判定日2026-09-12）の続行条件に近づいた形（監督は
  まだ表示ゼロ）。作品は0.83（08-19時点0.27から上昇）だが、**直近週（08-24週）は
  GSC最終日が08-29までのため実質6/7日分の途中データ**（`docs/operations.md`㉟と同型の
  落とし穴）。この表示を機械的に警告するPR #120（週次推移に`*`印を付ける）はまだ
  未マージなので、今回は手動で「途中週の数字だけで方針を変えない」を守った。
  ⑤(b)に`cloverworks アニメ一覧`(10.3位)・`crunchyroll アニメ一覧`(10.9位)・
  `engi アニメ一覧`(11.0位)など制作会社・サービス別ページ向けと見られるクエリが
  10〜11位に複数固まっている。09-12の判定に効きうるので次回以降も追う。
  ⑤(a)は`/anime/10143`（20表示・25.3位）の1件のみで20位台＝順位の問題（title起因ではない）。
  ⑥略称クエリは引き続き1クリックのみ（判定日2026-09-16は変更なし）。
- **`node scripts/freshness.js`**: 失敗なし。GSCの08-23・08-28は既知の回収可能な欠測。
  他の収集（サイト行動ログ・track-season）は健全。
- **`npx tsc --noEmit && node scripts/check.ts`**: 全件OK。新規の`⚠`なし
  （「descriptionの基準」の作品2件超過・「シーズンページのHTML量」は既知で変化なし）。
- **`content/analytics/site/2026-09-02.json`**: `rowCount`が0から36に増え、初めて実測が付いた
  （2026-08-19時点は`rowCount:0`で未実測だった）。未提携クリック（`officialClicks`）は
  直近30日で d_anime 4・netflix 4・bandai 2・unext/fod/dmm/niconico/disney 各1で、
  `docs/affiliate-setup.md`が既に最優先としているdアニメストアの判断と整合。
- **X**: Issue #76（週次成長キット・08-17週）はコメント無しのままclose、Issue #118
  （08-31週）もコメント0件。フォロワー・インプレッションは今回も取得できず。
- **アフィリエイト**: `content/affiliate/programs.ts`は引き続き3サービス
  （ABEMA・Prime Video・Hulu）で変化なし。
- **外向き通信**: プロキシが本番・Annictとも403で拒否、本番HTML実地検証は今回も不可。

## 気になった点（対応はユーザー判断）

`claude/daily-ops-*`ブランチのPRが2件、CI green のまま未マージで滞留している
（#114=08-31分、#120=09-01分。どちらもdoc-only or 小さな表示修正）。実質的な機能PR
（#113・#115〜#121）は正常にマージされているので運用が止まっているわけではないが、
この2件は放置すると次回以降さらに古くなる。マージ／クローズはユーザー判断
（`docs/daily-ops.md`5節の方針どおりセッション側では判断しない）。

## 目標の現在地（2026-11-06まで残り65日）

- Xフォロワー: 取得できず
- アフィリエイト成果件数: 取得できず（`/admin/analytics`はこの環境から到達不能）
- 提携済みサービス数: 3（ABEMA・Prime Video・Hulu、不変）

---

# 引き継ぎメモ（2026-09-01・seo-report.jsの週次推移に「途中の週」の印を足した）

## 前回（08-25）からの空白について

このファイルは08-25から更新されていなかったが、その間に本体は進んでいた
（`git log`で確認、コミットは全て`docs/operations.md`側に記録済み）:
- PR #113（08-31）: 声優ページの索引規則を実測で作り直し（今年は全部／過去年は総出演50作品
  以上）、次クールをsitemapに追加。詳細は`docs/operations.md`㉟。
- PR #115・#116（08-31）: 事前生成した制作会社・監督・声優（過去年）ページが本番で404を
  返していた件（`encodeURIComponent`の二重適用）の恒久対応。全件が事前生成に復帰し、
  ISR Writesの下限問題（㉝）も解消済み。詳細は`docs/operations.md`㊱。

**したがって08-25メモの「次の一手」のうち①③（過去クール事前生成・声優ページ拡張の答え合わせ）
は実質対応済み。** 未対応なのは下記「次回やること候補」のとおり。

## 今日やったこと

### 状況把握
- 外向き通信はプロキシで本番ドメインへのCONNECTが403（`gateway answered 403 to CONNECT`）。
  本番サイトへの実地検査は今回できず。`docs/daily-ops.md`の指示どおり、ローカルビルドでの
  検証に切り替えた（ただし今回の変更はNode CLIスクリプトのみでSSR/ISRページを触っていない
  ため、`next start`によるHTML確認は対象外）。
- `node scripts/seo-report.js`（GSC最終日2026-08-29）を実行し、②で表示急増（前半14日412表示→
  後半14日7130表示）に対し順位・CTRはやや悪化（22.6位・1.2%）と出た。

### 見つけたこと: `partial`フラグがデータにはあるのに、毎日読む画面に出ていなかった
2026-08-31の㉟対応で`weeklyByType`に`days`/`partial`（週の途中データ検知）を追加したはずだが、
それを実際に表示する`seo-report.js`④（面別の週次推移）には反映されていなかった。
`scripts/check.ts`の「途中の週に印を付ける」節はデータ層（`aggregateWeeklyByType`）だけを
検査しており、レポートの出力までは見ていなかった。

実際、今日のレポートで直近週（08-24週）は`作品`が6/7日ぶんの**途中**データのまま完全な週
（08-17週・7日ぶん）と並んで表示され、「直近2週の表示回数の増減」が`作品 +432`とだけ
出力していた。㉟と同じ間違いを別のセッションが再現しかけていた形（実害が出る前に発見）。

### 直した内容（PR、下記リンク）
1. `scripts/seo-report.js`の④に、途中の週（`partial:true`）のセルへ`*`を付け、注記を追加。
2. 「直近2週の増減」で比較する2週のどちらかが途中なら行に注記を付け、末尾に
   「傾向として扱わず、完全な週が揃ってから判断すること」の警告を出す。
3. `scripts/check-seo-report.js`に回帰テスト5件追加（途中週に`*`が付く／完全な週どうしは
   何も付かない、の両方を固定）。
4. `docs/operations.md`㉟に追記。

`npm ci && npm run check`（全節OK）、`rm -rf .next && npm run build`（成功）を確認済み。

## 目標の現在地（2026-11-06まで残り66日）
- Xフォロワー: 取得できず（週次成長キットIssueへの記入なし）
- アフィリエイト成果件数: 取得できず（`/admin/analytics`はこの環境から到達不能）
- 提携済みサービス数: 3（ABEMA・Prime Video・Hulu、不変）

## SEOの現在地（`seo-report.js` 08-29断面）
- ①: クリック94・表示7542・CTR1.2%・平均22.6位（28日ローリング。表示急増中）
- ③: 声优1ページあたり2.25クリックで引き続き最上位、作品0.83。ただし④の直近2週比較は
  今回の修正どおり「途中の週を含む」ため傾向として扱わないこと。

## 次回やること候補
1. **Vercel利用上限の30日枠の答え合わせ**（㉝の宿題）。08-25デプロイから1週間以上経過して
   いるはずなので、ユーザーにダッシュボードのISR Writes実測を確認してもらう。見込み
   （約68,000件/30日）との答え合わせがまだ`docs/operations.md`に記録されていない。
2. `app/anime/[id]/page.tsx`が関連作品8件・共演声優判定のためだけに`getSeasonData()`で
   クール全作品（最大224作品・casts込み）を読んでいる構造がまだ残っている（08-25メモの
   「次の一手」①、未着手）。軽量な索引への置き換えは表示の挙動が変わりうるので単独PRで。
3. `robots.txt`にCrawl-delayが無い件（未着手）。SEOとのトレードオフなので
   `docs/seo-operations.md`と突き合わせてから判断。
4. 9/2以降、08-24週が完全（7日ぶん）になったら、今回付けた`*`印が正しく消えるか・
   増減の数字が変わるかを実地で確認する（今回の修正の答え合わせ）。

## ユーザーにやってほしいこと
- Xアナリティクス（インプレッション・プロフィールアクセス・フォロワー数）の記入。
- dアニメストアなど未提携ASPの申請状況の確認（`docs/affiliate-setup.md`）。
- Vercelダッシュボードで30日枠（ISR Writes等）の実測確認（上記1）。

---

# 引き継ぎメモ（2026-08-31・日次巡回。コード変更は先行PR #113 で完了済み、今回はhandoff整理とPR清掃）

## 状況: 今日の実質作業はこのセッションの前に完了していた

このセッション開始時点で、既に別セッションが今日の日次巡回（GSC実測に基づく声優ページの
索引規則の作り直し・次クールのsitemap追加・診断の機械監視化）をPR #113として完了・マージ
済みだった（マージ 08-31 17:46 JST、CI green・詳細は`docs/operations.md`の㉟）。
そちらは`docs/handoff.md`の更新を欠いていた（`docs/daily-ops.md`5節の必須手順）ので、
このセッションはその欠けを埋め、副次的に見つかった残課題を片付けた。

## やったこと

1. **PR #113のCI結果を確認**: `main`ブランチの最新コミットでCI（`ci.yml`）が`success`。
2. **`node scripts/seo-report.js`を実行**（毎日必須。GSC最終日2026-08-27）:
   直近28日 クリック80・表示6,300・CTR1.3%・平均順位22.3位。面別1ページあたりクリックは
   声優2.25が最効率（作品0.65）。⑤⑥に新しい単発の手がかり無し（11〜20位クエリは表示数が
   小さいものばかり）。㉟の対応（索引規則の作り直し）が入ったばかりなので、次の判断は
   2週間の傾向が出てから（`docs/seo-operations.md`のルールどおり）。
3. **`npm ci && npm run check && rm -rf .next && npm run build`** 全件OK（新規の`⚠`なし。
   「descriptionの基準」の`⚠`2件は既知・前から出ているもの）。
4. **滞留していた古い日次巡回PRを2件クローズ**: PR #105（2026-08-26分）・PR #108（2026-08-27分）。
   どちらも`docs/handoff.md`のみを更新するdoc-onlyのPRで、当時マージされないまま`main`が
   大きく進み（PR #113 まで含む）、ベースが古くなっていた。中身は「コード変更なし・
   `npm run check`とビルドは全件OK」という当日の点検記録のみで、今日時点では歴史的価値しか
   ないため、統合はせずクローズのみ行った（本メモの下にある08-25以前の3世代がこのファイルの
   「直近3世代」の枠を占めているため、08-26・08-27の内容を新規に差し込むとその方針と衝突する）。
5. **本番・Annictへの外向き通信は今回も遮断**（プロキシが両ドメインへのCONNECTを403で拒否）。
   本番HTMLの実地検証はできなかった。

## 目標の現在地（2026-11-06まで残り67日）

- Xフォロワー: 取得できず（Issue #76は08-17から、Issue #90は08-23から更新無し）
- アフィリエイト成果件数: 取得できず（`/admin/analytics`はこの環境から到達不能）
- 提携済みサービス数: 3（ABEMA・Prime Video・Hulu、不変）

## 次回やること候補

1. ㉟の対応（声優ページ索引規則の作り直し・次クールsitemap追加）が実測でどう動くか、
   2週間分のGSCが溜まったら`seo-report.js`の④で確認する（目安 2026-09-14頃）。
2. Xアナリティクス（インプレッション・フォロワー数）の記入待ち（Issue #76/#90）。
3. dアニメストアの提携申請状況の確認（`docs/affiliate-setup.md`）。
4. `docs/operations.md`㉝の30日枠答え合わせ（判定目安2026-09-01頃・次回セッションで）。

## ユーザーにやってほしいこと

- Xアナリティクス（インプレッション・フォロワー数・プロフィールアクセス）の記入。
- 次に申請すべきアフィリエイトASP案件があれば`docs/affiliate-setup.md`の優先順位を参照。

---

# 引き継ぎメモ（2026-08-24〜08-25・Vercel利用上限超過とその対応）は`docs/operations.md`の㉝を参照

Vercel Hobbyの利用上限超過による本番停止（HTTP 402・約6時間以上、08-24）と、その対応
（一次対応: `vercel.json`の新設・ISR再検証の統一・middlewareの往復解消／根本対応:
再検証をタグでの指名方式に切り替え・事前生成の拡大、いずれも08-25）の詳細記録は
`docs/operations.md`の㉝「Vercelの利用上限を超えてサイトが停止した」に統合済み
（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。
