# 引き継ぎメモ（2026-09-20・本番SSR検査の誤検知を修正。日次巡回は久々に再開）

## 状況

`docs/handoff.md`の前回更新は09-15。その後09-16〜09-19は`claude/daily-ops-*`のPRが
無く、代わりに`claude/anime-guide-performance-n0vvh2`ブランチで大きめのインフラ対応
（PR #160・#162・#163。Vercel利用量の恒久対応・スナップショット再生成）が進んでいた
（`docs/operations.md`の[51]・[52]）。日次巡回の枠組みとしては空白だったが、
リポジトリ側の障害ではない（別セッションの直接作業）。

## 今日見つけて直したこと（PR #165）

**Issue #146が2026-09-09〜09-19の11日間、毎晩失敗し続けていた。** 09-09〜09-18の原因は
middlewareの400/404不具合（PR #154・09-19朝マージ済み）だったが、**09-19夜の実行でも
まだ失敗していた**。ログを取ると別の原因だった。

2026-09-20未明のスナップショット再生成（`castCredits`を全クール保存）で
`content/archive/index.json`の`castCreditsComplete`が全季節`true`になり、過去クールの
作品ページはAnnictに一切問い合わせない経路（①）に切り替わった。設計どおり
`fetchedAt`が`null`になり「配信情報の取得日」が表示されなくなった（㊻の意図した
**正しい挙動**）。ところが`scripts/verify-production.sh`のB節がこの文言の存在を
無条件に要求しており、**本番は正しいのに検査側が古いままで9日連続の誤検知**になっていた。

`archive-last-season-complete`（`scripts/lib/json-pick.js`）でそのクールの
`castCreditsComplete`を読み、B節をその値で分岐させた。`check-verify-production.js`にも
「①経路なのに誤って取得日を出す」変異が検出されることを追加。ローカル`next start`+`curl`
で`/anime/13180`が「取得日」も「確認日」も出さず正常描画することを確認済み。
`node scripts/check.ts`・`check-verify-production.js`・`npm run build`・
`check-build-size.js`（509.8MB/650MB=78%）・`check-page-css.js`は全てOK。

## SEO（`node scripts/seo-report.js`・GSC最終日2026-09-16）

直近28日: クリック+148・表示+13419（実数も伸びている。②で前半94/後半70クリックと
確認済み）。平均順位は悪化基調（20.6→25.3）が続くが、これは③④で継続的に指摘している
「長い裾（過去クール・声優ページ）の拡張による指標の希釈」の範囲内（新しい異常ではない）。
③面別効率: サービス別3.75・ランキング4.00（1ページのみ）・声優3.00・トップ1.50・作品1.17
（作品ページは引き続き最低水準＝量を増やす方向には投資しない、という既存方針は変わらず）。
⑤(b)で伸びしろが最大なのは「cloverworks アニメ 一覧」（81表示・10.6位）。制作会社面の
判定表（`docs/seo-operations.md`3節）は既に続行判定済みなので、今回は新規の対応なし。
判定期日が来ているものは無い（声優ページ索引方針の再判定は2026-10-12）。

## 無料枠・欠測検知

`usage-report.js`はHobbyプランにつき明細0件（[51]で確定済み・想定どおり）。
`freshness.js`は失敗0・警告2（既知）・既知の欠測21件のみで新規異常なし。

## 未マージPR

PR #165（今回）以外に滞留なし（前回確認できていた#130・#132・#137・#142は
いずれもクローズ済み/整理済み）。

## 次回やること候補

1. PR #165 のCIを見届けてマージする（このセッションから購読済み・`subscribe_pr_activity`）。
2. Issue #158「Vercel利用量の答え合わせ（2026-09-18・3倍枠が切れる前の最終確認）」は
   ダッシュボードの目視が必要でセッションからは完結できない。ユーザー対応待ち。
3. `docs/seo-operations.md`3節の判定表の再判定（声優ページ索引方針・内部リンク）は
   2026-10-12。まだ先。
4. Xアナリティクス・dアニメストア提携申請状況の確認（継続、今回も取得できず。
   Issue #164・#152ともコメント無し）。

---

# 引き継ぎメモ（2026-09-15・日次巡回が7日止まっていた件の発見と、その間に紛れ込んだ本番不具合の修正）

## ⚠️ 最優先: 日次巡回そのものが2026-09-08〜09-14の7日間動いていなかった

`claude/daily-ops-*`ブランチが09-07を最後に09-14まで作られておらず、日次巡回のPRも
その間0件だった（`docs/operations.md`㊿）。原因はこのセッションからは特定できない
（スケジュール実行の起動はリポジトリの外側にある）。**ユーザー側でスケジュール設定が
有効かどうかの確認が必要。** 別のcron（GSC・行動ログ・track-season等）は同期間も
正常に動いていたので、リポジトリ・CI側の障害ではない。

## 今日見つけて直したこと

1. **公開API・埋め込みが400を返すべき場面で404を返していた（㊾・重大度高・09-09〜09-14の6日間）。**
   2026-09-09にマージされたPR #138のmiddleware変更が、`/api/work/[id]`・
   `/embed/anime/[id]`を含む全ルートに一律404を返すようにしていた。この2窓口は
   従来「形が不正なIDには400」という契約を持っており（㊲）、`verify-production.sh`が
   本番で6日連続検知していたが（Issue #146）、上記の巡回停止で誰も見ていなかった。
   `lib/routeGuard.ts`にルートごとの`malformedStatus`を持たせ、`middleware.ts`で
   出し分ける形に修正。`scripts/check.ts`にstatusコードまで確認する検査を追加。
2. **AniList側のHTTP 403で`track-season.js`/`fetch-upcoming.js`が09-07〜09-12に
   失敗していた（自然復旧・09-13〜正常）。** 取り返せない欠測なので
   `scripts/freshness.js`のACKNOWLEDGEDに記録した。

## SEO（`node scripts/seo-report.js`・GSC最終日2026-09-11）

直近28日: クリック+117・表示+11828（28日ローリング、②で前半75/後半58クリックと
確認済み＝実数も伸びている）。平均順位は悪化基調が継続（③④は`seo-report.js`の
出力を都度見ること。ここには転記しない）。直近週（09-07週）は`partial`＝途中データ。
判定表の期日（監督09-12＝撤退条件該当、制作会社09-12＝続行）は`docs/seo-operations.md`
3節を参照。次回はこの2件を期日超過分として確定判断すること。

## 次回やること候補

1. **ユーザーにスケジュール設定の確認を依頼する**（最優先。7日間の空白の原因）。
2. 3節の判定表（監督ページの撤退・制作会社ページの続行）の確定判断。判定日09-12は
   既に過ぎている。
3. 未マージPRの滞留（#130・#132・#137・#142、いずれもCI green）。マージ判断はユーザー側。
4. Xアナリティクス・dアニメストア提携申請状況の確認（継続、今回も取得できず）。

---

# 引き継ぎメモ（2026-09-04・表示速度。「2秒以上」の本当の原因を本番実測で特定して直した）

## 依頼

利用者から「アニメ視聴ガイドの表示が2秒以上。**Vercelの無料枠内のまま**速くしてほしい」。
対象は**トップ／シーズン一覧**（他のページ種別は未検証）。追加条件が4つ:
①恒久的に無料枠を超えない ②2秒未満 ③既存システムに不具合を起こさない
④**クールが増えるたびに作品数が増えることを織り込む**。さらに「**メインはスマホユーザー**」。

## いちばん大事なこと（前回の見立ては外れていた）

利用者からPageSpeed InsightsのAPIキーをもらい、**本番を初めて実地で測れた**。結果、
㊴（09-03のPR）で削ったDOMノード・バイト数は体感の主因では**なかった**。

- LCPは**100%が element-render-delay**（TTFBは6〜16ms＝サーバーは速い）。
- LCP要素は`h1.brand`。つまり「一番上の見出しが出るまで」が遅い。
- 主コストは**スクリプト評価**。トップ **802ms** に対しシーズンページ 469ms。
  逆にHTMLのパースはトップ **8ms**・シーズンページ 50ms。
  **「HTMLを読む」より「JSで組み立て直す」ほうが桁違いに高い。**

原因は2つ。どちらも**画面を見ている限り絶対に気づけない**。

1. **トップページ "/" のサーバーHTMLが空だった。** `useSearchParams()`を呼ぶ
   `TopPageExplorer`があったため、Next.js 14がSuspense境界を丸ごとクライアント描画へ
   退避させ、本番HTMLは`<div class="wrap"></div>`だけ（h1が0個・作品リンク0件）。
   2026-08-05に`/season/**`は直したのに、**トップだけ「ディープリンクに必要」として
   残していた**。サイトの正面玄関＝canonicalの指す先が1ヶ月間、空だった。
2. **CSSが外部ファイルだった。** `<link rel="stylesheet">`のせいでHTMLが届いた後に
   もう1往復。ローカル実測で約**370ms**（PSIの見積もりは150ms）。

## やったこと（PR: `claude/anime-guide-performance-n0vvh2`・コミット3件）

詳細と数字は`docs/operations.md`の㊴（09-03分）と㊵（09-04分）。

1. **トップのサーバー描画を復旧**。`useSearchParams`を捨て、`TopPageExplorer`が
   **hydrationより前に**`window.location.search`から1度だけクエリを読む。
   `SeasonExplorer`は**初期描画では使わず**マウント後の`useEffect`で反映する
   （初期描画で使うとサーバーHTMLと食い違い、クエリ付きで開いた人にだけhydrationが壊れる）。
2. **CSSをHTMLに焼き込んだ**。`scripts/build-inline-css.js`が`app/globals.css`を
   `app/inlineCss.ts`へ変換し、`predev`/`prebuild`で自動再生成。ミニファイアは
   「壊れると全ページ無スタイル」なので賢い最適化をしない（コメント除去と空白畳みだけ）。
3. **サムネイルをWebPに**（110枚・4.11MB→2.51MB＝**−39%**、見た目は同じ）。
   `sharp`は依存に入れない（再生成時だけ`npm install --no-save sharp`）。
4. **逆戻り防止の検査を3節追加**（`scripts/check.ts`）＋`verify-production.sh`に
   **A0節**（トップのh1・作品リンク・`rel="stylesheet"`が無いこと・`<style>`があること）。
   6通りの壊し方で落ちることを確認済み。

## 効果（ローカル本番ビルド・5回の中央値・CPU4倍/1.6Mbps/390×844のスマホ相当）

| ページ | 変更前 LCP | 変更後 LCP |
|---|---|---|
| `/season/2025/summer`（173作品） | 992ms | **472ms（−52%）** |
| `/season/2010/spring`（93作品） | 824ms | **400ms（−51%）** |
| `/anime/14876` | － | 320ms |

トップは変更前の数字が取れない（**HTMLが空でLCP要素が存在しなかった**）。
h1は0個→1個、作品リンクは0件→今期の全作品に復帰した。

## ④「作品数が増える」への答え（推測ではなく実測）

- 作品1件あたり **1.0ms・約4.6KB**（93作品400ms／173作品472ms から導出）。
- 500作品でも**約810ms**の見込み＝2秒には当たらない。
- それでも気づけるように、`verify-production.sh`に**シーズンページHTML 2MB
  （約430作品）で落ちる**しきい値を入れた。

## 無料枠への影響

- **ISR Writesは1件も増えない**（`revalidate`もページ数も変えていない）。
- トップのSSRが復活したぶんのCPUは**月あたり約22秒＝4時間枠の0.15%**。
- 転送量はHTML +8KB／回だが、**CSSのリクエストが1本減り、画像は39%小さくなる**。

## 残っていること

- **本番での答え合わせ（PSI）が未実施。** この環境から本番URLへ出られないため、
  **マージ後に本番を測る**方針で利用者と合意済み（2026-09-04）。
  測る対象はトップ／`/season/2025/summer`／作品ページの3面と、CrUXの実ユーザー値。
- ㉝のチェックポイント（2026-09-18）で無料枠3指標の実測を確認する。

## 見送った案（理由つき。再提案の前に読むこと）

- **flightペイロードの圧縮**（services/castNames）… バッジ・アフィリエイトの経路
  （重大度高）を触るのに、HTML全体の7.5%しか減らない。
- **折りたたみパネルの遅延描画** … パネルはDOMの1.4%しかない。
- **`legacy-javascript`のbrowserslist絞り込み** … 12KiBのために対応ブラウザを狭める。
- **カード一覧のイベント委譲化** … 効果は最大だが「不具合を起こさない」条件下で
  最も壊すリスクが高い。

---

# 引き継ぎメモ（2026-09-03・日次巡回・コード変更なし）は統合済み

要点: 未マージPR4件（#120・#124・#126・#127）が滞留していたが、いずれも後日マージ・
整理済み（`docs/operations.md`の㉝以降を参照）。GSC断面は転記しない方針＝当時の
`content/analytics/gsc/`のJSONを参照（「直近3世代だけ残す」の運用ルールに従い本文を圧縮）。

---

# 引き継ぎメモ（2026-09-02・コード変更なし。Vercel利用量の答え合わせIssue #123待ちを記録した）は
統合済み（「直近3世代だけ残す」の運用ルールに従い本文を圧縮）。要点: Issue #123は
ユーザーのVercelダッシュボード確認待ちのまま。提携済みアフィリエイトは3サービス
（ABEMA・Prime Video・Hulu）。数字はコード変更を伴わないため`docs/operations.md`への
対応する番号は無い（GSC断面は転記しない方針＝`content/analytics/gsc/`の当時のJSONを参照）。

---

# 引き継ぎメモ（2026-09-01・seo-report.jsの週次推移に「途中の週」の印を足した）は`docs/operations.md`の㉟を参照

GSCの3日ラグで最新週が必ず途中までしか埋まらない件に対し、`weeklyByType`の各行に
`days`/`partial`を持たせ、レポート表示にも`*`を出すようにした（PR #120）。詳細記録は
`docs/operations.md`の㉟「㉞の診断が部分的に誤っていた件と、その訂正」に統合済み
（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。

---

# 引き継ぎメモ（2026-08-31・声優ページの索引規則の作り直し・次クールsitemap追加）は`docs/operations.md`の㉟を参照

GSC実測に基づく声優ページの索引規則の作り直し（今年は全部／過去年は総出演50作品以上）、
次クールをsitemapに追加、診断の機械監視化（PR #113）の詳細記録は`docs/operations.md`の
㉟「㉞の診断が部分的に誤っていた件と、その訂正」に統合済み（「直近3世代だけ残す」の
運用ルールに従い、このファイルからは本文を削除）。

---

# 引き継ぎメモ（2026-08-24〜08-25・Vercel利用上限超過とその対応）は`docs/operations.md`の㉝を参照

Vercel Hobbyの利用上限超過による本番停止（HTTP 402・約6時間以上、08-24）と、その対応
（一次対応: `vercel.json`の新設・ISR再検証の統一・middlewareの往復解消／根本対応:
再検証をタグでの指名方式に切り替え・事前生成の拡大、いずれも08-25）の詳細記録は
`docs/operations.md`の㉝「Vercelの利用上限を超えてサイトが停止した」に統合済み
（「直近3世代だけ残す」の運用ルールに従い、このファイルからは本文を削除）。
