# スナップショット再生成の手順（2026-08-11）

過去クール（2010〜2025年）のデータをAnnictから取り直して、
`content/snapshots/` を作り直すための手順書です。

**PCでの作業です**（Annictへの通信とトークンが要るため、Claudeのセッションからは実行できません）。
所要時間は**30分〜1時間**、うち大半は待ち時間です。

---

## なぜやるのか

シーズン一覧のクエリが声優を**先頭5人しか取っていなかった**不具合（`docs/operations.md` の⑳）を
2026-08-11に直しました。ただし `content/snapshots/` の中身は**旧設定で作られたまま**なので、
過去クールは直っていません。

現状：**声優データがある4,121作品のうち3,622作品（87.9%）が、ちょうど5人で切られています。**

この作業をすると、次が一度に直ります。

| 直るもの | いま |
|---|---|
| 過去クールの検索（声優名） | 6番目以降の声優では出てこない |
| 過去クールの声優ページ | 出演作が抜ける・ページ自体が消えている人がいる |
| sitemapに載る声優ページ | 1,413ページ（本来はもっと多い） |
| 制作会社・監督の索引 `studios.json` | **空**（旧形式に `roleCredits` が無いため） |

---

## 事前に用意するもの

### ① Node.js 22 以上

コマンドプロンプト（または PowerShell）で確認します。

```
node -v
```

`v22.x.x` 以上と出ればOKです。`v20` などと出た場合は https://nodejs.org/ から
**22 以上**を入れ直してください（このリポジトリのスクリプトは `.ts` ファイルを
直接実行するため、Node 22 の機能が必要です）。

### ② ANNICT_TOKEN

リポジトリのフォルダ直下に `.env.local` というファイルが必要です。

**すでにあるか確認する**（ファイル名が出れば存在します。中身は表示しません）：

```
dir .env.local
```

**無い場合**は次の手順で作ります。

1. https://annict.com/settings/tokens を開く
2. 「Read」権限でトークンを発行する
3. リポジトリのフォルダ直下に `.env.local` という名前のテキストファイルを作る
4. 中身を次の1行にする（`xxxxx` の部分を発行したトークンに置き換え）

```
ANNICT_TOKEN=xxxxx
```

> `.env.local` は `.gitignore` 済みなので、コミットされることはありません。

---

## 手順

以下、コマンドはすべて**リポジトリのフォルダの中**で実行します。
最初にそこへ移動してください（パスはご自身の環境のものに置き換え）。

```
cd C:\Users\user\animedia
```

### 手順1：修正を取り込む【いちばん重要】

**この手順を飛ばすと、作業しても何も直りません。** 古いコード（5人しか取らない）で
再生成することになり、まったく同じ結果が出るだけです。

```
git switch main
git pull origin main
```

取り込めたことを確認します。

```
node scripts/check.ts
```

出力の中に次の行があればOKです。

```
✓  一覧クエリのキャスト件数が十分    → CASTS_LIST=40（下限20）
ℹ  スナップショットの切断率        → ちょうど5件 3622/4121作品 (87.9%)  ← 旧設定のまま。…
```

`CASTS_LIST=5` と出た場合は `git pull` ができていません。手順1をやり直してください。

> 【もし `git pull` でエラーが出たら】
> 「Your local changes would be overwritten」と出る場合、手元に未保存の変更があります。
> `git stash` を実行してから `git pull origin main` をやり直してください。

### 手順2：スナップショットを再生成する（30分程度・待つだけ）

```
node scripts/snapshot-past-seasons.ts 2010 2025 --force
```

- `--force` は「既存ファイルを上書きする」指定です。**これが無いと全部スキップされます**
- 64クール（16年 × 4クール）を1つずつ取り直します
- Annictに負荷をかけないよう1クールごとに0.8秒待つので、時間がかかります
- **途中でウィンドウを閉じないでください**

進行中はこのように1行ずつ出ます。

```
✓ 2010-winter: 48 作品 → C:\Users\user\animedia\content\snapshots\2010-winter.json
✓ 2010-spring: 52 作品 → ...
（…64行…）

完了: 生成 64 / スキップ（既存）0 / 失敗 0
```

**「失敗 0」であることを確認してください。**

> 【`✗` が出た年があった場合】
> **そのままもう一度実行しても直りません。** 失敗した年は古いファイルが残っているため、
> `--force` を付けずに実行するとスキップされてしまいます。
> 失敗した年を指定して、`--force` 付きで取り直してください。
>
> ```
> node scripts/snapshot-past-seasons.ts 2017 2017 --force
> ```
>
> （`2017` の部分を、`✗` が出た年に置き換えます。複数年あれば1年ずつ繰り返します）

### 手順3：3つの索引を作り直す

スナップショットから作られている索引が、古いままだとズレます。3つとも実行します。
それぞれ数秒で終わります。

```
node scripts/build-archive-index.ts
node scripts/build-person-index.ts
node scripts/build-studio-index.ts
```

それぞれ次のような1行が出ます（数字は増えているはずです）。

```
✓ .../content/archive/index.json
  シーズン 64 件 / 総作品 8957 件 / sitemap掲載対象（配信1件以上）1961 件
content/archive/people.json を書きました: 787人 / 出演7721件
content/archive/studios.json を書きました: 制作会社0社/出演0件・監督0人/出演0件
```

3つ目（`studios.json`）は**いまは0件ですが、再生成後は0でなくなります**。
これが埋まることが、スナップショットが新形式になった証拠にもなります。

### 手順4：結果を確認する

```
node scripts/check.ts
```

**成功していれば、切断率の行が大きく下がっています。**

```
ℹ  スナップショットの切断率   → ちょうど5件 210/4121作品 (5.1%)
```

（0%にはなりません。本当に声優がちょうど5人の作品もあるためです。
**87.9%から大きく下がっていれば成功**です。目安として20%以下になります。）

あわせて、出力全体に `✗` の行が無いことを確認してください（`✓` と `ℹ` だけならOKです）。

### 手順5：保存して反映する

生成されたファイルをコミットして送ります。

```
git add content/
git commit -m "スナップショットを再生成（声優データの取りこぼしを解消）"
git push origin main
```

pushが通れば、数分でVercelに反映されます。

---

## 終わったら

`node scripts/check.ts` の出力（特に**切断率の行**）をそのまま貼って教えてください。
`people.json` の人数と出演件数がどれだけ増えたかを確認し、sitemapに載る声優ページの
件数を測り直します。

---

## よくあるつまずき

| 症状 | 原因と対処 |
|---|---|
| `ANNICT_TOKEN が未設定です` | `.env.local` が無いか、置き場所が違う。**リポジトリのフォルダ直下**に置く |
| `Unknown file extension ".ts"` | Node が古い。`node -v` で 22 以上か確認する |
| 何も起きずすぐ終わる（生成 0 / スキップ 64） | `--force` を付け忘れている |
| `対象は過去年のみです` と出る | 終了年に今年（2026）を指定している。`2025` までにする |
| 途中で `✗` が出た | 上の「`✗` が出た年があった場合」を参照。**年を指定して `--force` で取り直す** |
| `git push` が拒否される | 先に `git pull origin main` を実行してから push し直す |
| `MODULE_TYPELESS_PACKAGE_JSON` という警告が出る | **無視してよい**。毎回出る警告で、失敗ではない |

## 次に同じ作業が必要になるとき

- **年が変わったとき**（例：2027年になったら）。ただしそのときは前年分だけでよく、
  `--force` も不要です。

  ```
  node scripts/snapshot-past-seasons.ts 2026 2026
  ```

  そのあと手順3・4・5は同じように行います。
- スナップショットに入れる項目を増やしたとき（今回のような全件再生成）。
